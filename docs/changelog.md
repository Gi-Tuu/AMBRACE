## 2026-08-29（v3.3.0 发布：MCP 稳定版 + 流式/TTS 全面修复）
| 内容 | 原因 |
|---|---|
| 发布 v3.3.0（3.3.0+18）：MCP 四阶段稳定版（多用户隔离/流式防幻觉/连接锁/超时可配/资源注入）、语音逐句播放修复（打断后能继续多句）、回退消息不再错删历史、全量审查 V2 全项修复 | MCP 是核心新能力，多轮审查修复到位，统一发稳定版；release 包已脱敏扫描 |
| Release 附件：APK（3.3.0+18）+ ai_companion_public.zip（含 bge-m3 模型）；GHCR 镜像随 tag 自动构建 | 发布流程标配 |

## 2026-08-28（v3.2.9 发布：MCP 标准工具接入 + 语音与主动消息增强）
| 内容 | 原因 |
|---|---|
| 发布 v3.2.9（3.2.9+17）：MCP 标准协议接入四阶段（stdio/远程传输、AI 调用 MCP 工具、扩展页管理、资源注入/调用日志）、插件开发文档正式版、多轮审查修复（统计接口/测试接口/Alembic 补表/TTS 链路）等全部今日改动 | MCP 是大功能主线，统一发版；release 包已脱敏扫描 |
| Release 附件：APK（3.2.9+17）+ ai_companion_public.zip（含 bge-m3 模型）；GHCR 镜像随 tag 自动构建 | 发布流程标配 |

## 2026-08-24（v3.2.8 发布：逐句语音回复 + 主动消息增强 + 流式修复与用量记账）
| 内容 | 原因 |
|---|---|
| 发布 v3.2.8（3.2.8+16）：逐句语音（语音模式 AI 回复边打字边出声、顺序播放可打断）、主动消息增强（自然度评分 / 用户作息学习 / 手动触发测试接口）、SSE 流式用量记账、SSE 错误友好化 / 防重复弹错 / 反代兼容、API Key 保存自动生效等全部今日改动 | v3.2.7 后累积修复 + 三个新功能，统一发版；release 包已脱敏扫描 |
| Release 附件：APK（3.2.8+16）+ ai_companion_public.zip（含 bge-m3 模型）；GHCR 镜像随 tag 自动构建 | 发布流程标配 |

## 2026-08-24（v3.2.7 发布：真 SSE 流式回复 + Onboarding + Alembic + 新机部署修复全套）
| 内容 | 原因 |
|---|---|
| 发布 v3.2.7（3.2.7+15）：真 SSE 流式回复（逐 token + 增量语义切块，深度思考/插件改写/流式异常三场景回退）、Onboarding 四步引导、Alembic 数据库迁移、记忆衰减曲线可视化、多语种收尾、新机部署 Bug 报告 P1×6+P2×5、控制台仪表盘修复、小家 404 空状态、首账号自动主账号等全部 2026-08-24 改动 | 今日改动量大（部署修复 + 五个产品/基建任务），统一发版；release 包已脱敏扫描 |
| Release 附件：APK（3.2.7+15）+ ai_companion_public.zip（含 bge-m3 模型）；GHCR 镜像随 tag 自动构建 | 发布流程标配；download_models.py 动态 latest 依赖 zip 附件 |
## 2026-08-24（SSE 真流式回复 + 语义切块合并）
| 内容 | 原因 |
|---|---|
| 新增：SSE 真流式端点 `POST /api/v1/chat/sessions/{id}/messages/stream`（`text/event-stream`），主私聊链路改为「边生成边上屏」，不再等完整回复 | 原链路为「单次 LLM 生成完整回复 → split_response 切块 → 一次性循环推送」的『切块假流式』，用户要等完整回复；真流式让回复逐字打在气泡上 |
| 事件流：`data: {type:user_message}`（用户消息落库回传）/ `{type:delta,text}`（增量展示文本，打字机）/ `{type:block,id,content,created_at,extra_meta}`（完整语义块，已落库）/ `{type:done,message,blocks,memories_updated}` / `{type:error,detail}` / `{type:cold_war,message}` | 增量与确认解耦：delta 打字机、block/done 确认替换；消息模型零迁移（块走既有 ChatMessage 分块与 extra_meta） |
| 流式生成：`_run_agent_core` 加 `stream_sink` 模式，注入 LangGraph state；`generate_response` 生成节点改调既有 `chat_completion_stream`（OpenAI 兼容逐 delta，语音链路在用） | 复用既有流式能力，不新增客户端；流式块经增量语义切块器 `IncrementalResponseChunker` 边收边切 |
| 增量语义切块：新增 `IncrementalResponseChunker`（句末标点 + 3 句/80 字成块；情绪态整段不拆；生成结束 flush 末块；未闭合 `【`/`[`/`（` 保持块边界单调不闪退），与 `split_response` 行为对齐 | 边流式边按句子/情绪边界切完整块，落库块与展示层一条线；完整块走既有落库/推送逻辑 |
| 回退：三场景回退现有非流式 chunked——①深度思考 `reasoning_level=2`（生成节点判定后回退非流式）②存在启用中的插件 `after_generate` 改写钩子 ③LLM 流式异常（推 `error` + 服务层回退 `send_and_receive_chunked`） | 深度思考/插件改写需完整回复语义，无法增量；流式异常不黑屏，自动走非流式重试保命 |
| 前端：`chat_api` 加 `streamMessage`（Dio `ResponseType.stream` 逐 `data:` 行 + `parseSseDataLine` 解析）；`chat_provider` 加 `handleStreamEvent`（delta 追加当前气泡打字机、block/done 确认替换、error 提示、cold_war、错误自动回退非流式 WS）；`message_bubble` 加 `isStreaming` 闪烁光标 | 与现有消息列表/历史兼容；流式气泡为本地临时消息，block 确认后替换为真实已落库消息 |
| 测试：后端 688 全绿（新增 test_incremental_chunker 11 + test_chat_stream 8）；flutter analyze 0；flutter test 全绿（新增 sse_stream_test + message_bubble_stream_test） | 增量切块边界/情绪/flush、流式端点 delta/done/error 回退、SSE 解析、流式光标均有测试 |
| 版本：维持 3.2.6+14 | 不引入数据库/接口破坏性变更，兼容既有历史与消息模型 |

## 2026-08-24（Onboarding 首次使用引导：3-4 步 + 内嵌 API Key + 自动首条消息）
| 内容 | 原因 |
|---|---|
| 新增：App 首次启动（无已保存服务器地址）进入 3-4 步引导——① 连接服务器（复用 ApiClient baseUrl 配置与连接测试）② 登录/注册 ③ 创建首个 AI 角色（名称+一句话性格）④ 内嵌 LLM API Key 配置（可跳过）；完成后自动创建角色会话并发送首条消息「你好」，进入聊天页 | 新用户输 IP→注册→建角色→找设置配 Key→回来聊天全靠摸索；引导把关键路径收敛到 3-4 步，最后一步内嵌配置，完成即聊 |
| 触发时机：SettingsProvider.needsOnboarding = !onboarding_done && serverUrl 为空（已完成/已配置用户直接进主页不打扰，重启不再显示） | 仅全新设备引导，已完成则跳过；完成标志持久化 shared_preferences（onboarding_done） |
| UI：独立 onboarding_screen.dart（StepIndicator 四步 + 可返回上一步 + API Key 步骤可跳过——跳过引导到设置页），风格对齐 Design Token | 与既有 iOS 风格一致，不引重型组件 |
| l10n：新增 onboarding* 文案 zh/en 成对（app_zh.arb/app_en.arb + gen-l10n），复用既有 serverAddress/username/password/login/register 等 | 多语种随既有 ARB 体系 |
| 设置抽屉「关于组」新增「重新运行首次引导」入口（带确认，仅登录后可见） | 便于已配置用户重看/重走（低优先可选） |
| 测试：新增 onboarding_logic_test（5 例触发时机/持久化）+ onboarding_screen_test（4 例步骤渲染/连接测试/切换/返回）；flutter analyze 0 / test 188 全绿 | 触发与逐步逻辑可单测；未新增后端改动（复用既有角色/聊天/api-config 接口串行调用） |
## 2026-08-24（数据库升级机制改造：版本链迁移，存量与新装平滑）
| 内容 | 原因 |
|---|---|
| 升级：数据库升级引入「版本链」机制——旧数据启动时自动对齐到新结构、全新安装按版本自动建表，升级过程可追溯、更稳、不丢数据 | 原数据库升级语句越堆越多、跨版本升级难追溯，风险高 |
## 2026-08-24（多语种零星收尾 + 记忆衰减曲线可视化）
| 内容 | 原因 |
|---|---|
| i18n：登录页忘记密码改密流程 / AI 内心世界记忆召回副标题 / 「小家」/「使用教程」/扩展表单占位示例 / 主题色与主题模式标签全部转 l10n（zh/en 成对），移除 app_theme.dart 硬编码色名/模式名 | 数量不多、纯 UI 文案，走既有 ARB 体系零星收尾 |
| 记忆衰减曲线：记忆详情页新增「记忆衰减曲线」视图（CustomPaint，横轴未来 30 天保留率、纵轴 0-100%，R=exp(-Δt/S)，S=strength_days），展示当前保留率 / 强度 S / 下次复习 / 复习次数 / 锁定冻结状态 | 让用户直观看到每条记忆的强度变化趋势（艾宾浩斯），定位删除倒计时/锁定原因；不引重型图表库，先做单条记忆 |
| 后端：list_memories / GET /{id} 补充只读返回 strength_days / last_reinforce_at / next_review_at / review_count；MemoryResponse schema 同步 | 前端曲线需要这些字段；只读扩展，不改衰减/写入逻辑 |
| 测试：新增 memory_decay_test.dart（保留率/流逝天数/曲线/锁定冻结/下次复习偏移 15 例）；flutter analyze 0 / test 179 全绿 | 纯逻辑可单测，覆盖艾宾浩斯公式与锁定语义 |

## 2026-08-24（新机部署 Bug 报告收尾：P1×6 + P2×5 修复）
| 内容 | 原因 |
|---|---|
| P1-1：chat.py API Key 错误匹配改大小写不敏感（lower() + authentication/invalid 兜底），填错 Key 返回 400 中文提示而非 500 internal error | DeepSeek 返回 "api key"（小写），原 "API Key" 匹配漏掉 |
| P1-2：setup.bat/setup.sh 全部路径加引号 + %~dp0/SCRIPT_DIR 绝对化，支持含空格/中文路径 | 新机部署到 C:\Users\John Doe\或中文路径会失败 |
| P1-3：docker_entrypoint.sh 兜底——/app/.env 是目录（Docker 自动创建）时改名保留，缺失时从 .env.example 生成 | 直接 docker compose up 且未跑 setup 时 .env 变目录、配置全失效 |
| P1-4：init_db.py / check_cognition_health.py 加 backend/ 到 sys.path（可直接 python scripts/init_db.py 运行） | 两脚本直接运行 ModuleNotFoundError |
| P1-5：download_models.py 增加 hf-mirror.com 国内镜像（HF 官方失败自动切换）+ 失败时中文镜像指引 | 国内 GitHub/HF 不可达时模型（记忆必需）下载失败无路可走 |
| P1-6：新增 backend/requirements-voice.txt（faster-whisper）+ 公开包 README 语音说明 + speech_service 失败日志带安装命令 | 语音转写为可选功能但此前静默失败无指引 |
| P2-1：docker healthcheck 改用 /health（进程存活），模型就绪由 /ready 单独暴露 | 模型缺失时容器不再误标 unhealthy |
| P2-3：get_server_info.py 过滤 169.254.x.x 链路本地地址 | 无网络时返回 APIPA 地址误导用户 |
| P2-4：uninstall.sh Linux/macOS 用 fuser/lsof 停 8000/8766（server_manager 仅 Windows 有效） | Linux/macOS 卸载后服务仍在运行 |
| P2-5：新增 scripts/ambrace.service systemd 单元模板 + setup.sh 结尾提示 | Linux 用户缺系统级守护（Windows 有 watchdog） |
| P2-2（报告项）已由动态 latest URL 修复覆盖，确认无需重复处理 | 见 2026-08-24 观察项收尾节 |
## 2026-08-24（发布观察项收尾：模型下载动态 URL / Docker 首启 healthcheck / 测试警告静音）
| 内容 | 原因 |
|---|---|
| 修复：download_models.py 不再硬编码 v3.2.0 下载 URL——改为动态解析 GitHub latest release 的 ai_companion_public.zip 附件（GitHub API），失败回退 HuggingFace；v3.2.6 Release 已补传 zip（含 bge-m3） | 观察项：URL 指向旧版本；动态解析后每次发版上传 zip 即自动生效，免维护版本号 |
| 修复：docker-compose.yml / docker-compose.ghcr.yml 首启 healthcheck start_period 20s→300s、retries 3→5 | 观察项：GHCR 首启自动下载 bge-m3（542MB）阻塞 uvicorn，20s 必误判 unhealthy |
| 确认：本地未跟踪 .env 为正确行为（.gitignore 排除 + setup 自动复制 .env.example），无改动 | 观察项：密钥不入库是安全设计 |
| 修复：pytest 静音 chromadb 第三方 DeprecationWarning（pyproject filterwarnings，按模块精确匹配）；aiosqlite 线程泄漏为偶发真实问题保留不静音 | 观察项：测试输出干净；全量 669 passed |
| 登记：Onboarding 首次引导 / Alembic 迁移 / 记忆衰减曲线可视化 / SSE 核实现状 四项新计划（详见 docs/plans.md） | 外部更新建议评审后按现状筛选 |
## 2026-08-24（控制台仪表盘空白修复：布局挤压 + 刷新链路异常可见）
| 内容 | 原因 |
|---|---|
| 修复：刷新后台线程 job() 不再用 except:pass 吞异常——改为分步 _safe 降级（每项失败给默认值，队列必有结果）+ traceback 回显 stderr + 状态栏提示「刷新异常」 | 新电脑部署实测：仪表盘 6 个状态框（服务器状态/健康/Ollama/角色数/记忆数/Token）空白，刷新结果未送达；吞异常使空白无法诊断 |
| 修复（根因）：仪表盘 6 个状态卡片内容不可见——grid 行高被「近 7 天 Token 用量」趋势卡（canvas 190px + SP_LG 内边距）挤压到 ~49px，而卡片 inner 垂直内边距 2×SP_LG=48px 直接把内容压成 1×1（mapped=0） | 新/旧电脑均实测 6 框空白；本地实例化控制台验证 widget 全部 mapped=0 |
| 修复：趋势图高度 190→60、趋势卡内边距 SP_LG→SP_XXS、卡片 inner 垂直内边距 SP_LG→SP_MD、主值字号 22→20 | 重新平衡布局：1000×640 与最小 840×560 窗口下 12 个标签全部可见、二次刷新稳定 |
| 修复：主线程 _poll 对 _apply_refresh 加 try/except 保护，单次异常不再终止整个刷新循环（after 链保持） | 避免单个 widget 异常导致后续刷新全部停止 |
| 调整：仪表盘卡片初始占位「…」改为「—」 | 明确占位语义，避免误读为空白 |
## 2026-08-24（新部署修复：首个账号自动成为主账号）
| 内容 | 原因 |
|---|---|
| 修复：注册接口在 users 表尚无任何主账号（is_admin=1）时，把第一个注册的账号自动设为主账号 | 全新部署时第一个账号默认 is_admin=0，而主账号管理入口又需要主账号权限，形成「无主账号死锁」；新电脑部署实测反馈 |
| 修复：启动迁移种子逻辑增强——无主账号时优先按 env ADMIN_USER_IDS 指定且存在的用户授权；若未指定或 id 不存在，自动把最早注册用户（MIN(id)）设为主账号 | 旧逻辑只在启动时按 id 批量 UPDATE，注册先于重启时补不上，且依赖 id=1 假设；新逻辑任何启动都能自愈 |
## 2026-08-24（v3.2.6 发布：权限管理三 tab + 织网 3D + SDK 升级）
| 内容 | 原因 |
|---|---|
| 发布 v3.2.6（3.2.6+14）：包含权限管理三 tab（AI 能力/主账号/服务器功能管理）、织网 3D P0-P3 全量（默认开、卡片纹理、精确拾取、手动档位切换、低端机分档降级）、SDK 升级（Flutter 3.47.1）等全部 2026-08-24 改动 | 今日改动积累较多，统一发版；release 包已脱敏扫描 + 隐私排除 |
| Release 附件：release APK（3.2.6+14）；GHCR 镜像随 tag 自动构建 | 发布流程标配 |

## 2026-08-24（织网 3D 手动档位切换）
| 内容 | 原因 |
|---|---|
| 新增：织网画布页顶部手动渲染档位选择器（SegmentedButton 四选一：全自动/3D 全量/3D 轻量/2.5D），放在维度筛选条上方一行，横向可滚动小屏不溢出 | 自动降级（帧率检测 full→light→2D）可能把 3D 降得不符合预期；给用户手动档位开关，用户手选优先 |
| 新增：渲染档位模式 enum（auto=全自动/full3d=3D 全量/light3d=3D 轻量/twoD=2.5D）；优先级：手动优先于 weave_3d flag 与自动降级状态 | 明确手动优先语义，避免与自动降级/flag 冲突 |
| 新增：持久化（shared_preferences key=weave_view_mode）保存用户所选模式，App 重启保持；首次默认 auto | 用户偏好跨会话保持 |
| 优化：手动模式（full3d/light3d/twoD）禁用自动降级检测（WeaveRenderDegrader 不参与，切回 auto 重新启用并从头暖机）；light3d 初始即 light 档（不预热纹理）；full3d 强制 3D 全量 | 手动档位固定，不因帧率/节点数自动变档 |

## 2026-08-24（织网 3D 低端机体验修复：2.5D 放开旋转 + 分档降级）
| 内容 | 原因 |
|---|---|
| 修复：2.5D 视图也放开 theta（`_WeaveScene2DHost` initState `setThetaLimit(null)`），可自由旋转翻到球背面 | 低端机一进织网就降到 2.5D，但 2.5D 仍把 theta 限在 ±1.35（约 ±77°），转不动、卡手（根因：3D 放开、降级后 2.5D 又限回）；现与 3D 一致自由翻转 |
| 修复：2.5D 背面淡化下调（`nodeDepthOpacity` backFar 0.30→0.22）；`projectPoint` 背面透视收敛（w=1-perspective·z）与 `edgeAlpha` 深度淡化已核对 | 2.5D 放开后可看到整个后半球，backFar 调低让背面更淡、翻转不刺眼、聚类泡/连线不错乱 |
| 新增：3D 分档降级 full→light→2D（full=纹理+连线+节点球；light=纯色圆点+连线+低细分球+聚类泡）。full 持续约 2s <30fps 降 light（提示「已切换轻量模式」）；light 持续约 2s <20fps 才回退 2.5D（提示「持续低帧率」）；light 不自动升回 full（一次性下坡，避免抖动） | 低端机此前直接一步掉到 2.5D，3D 体验没得玩；分档后低端机先在 3D 内降档保住 3D，撑不住才回 2.5D |
| 优化：light 档节点球用低细分共享几何体（`SphereGeometry` segments 8/rings 6）；light 档不预热纹理、清空纹理缓存。full 档细分保持 12/8（贴卡片纹理需保球面圆滑），纹理预热批次保持 3 张/帧 | 低端机降 light 后省顶点与纹理生成开销；full 档视觉与性能权衡见 dev-changelog |
| 新增：l10n「已切换轻量模式（3D 简化渲染）」提示（zh/en），与现有 `weaveFallback2D*` 区分 | 区分「降到 light（仍 3D）」与「回退 2.5D」两种提示，真机反馈可定位 |

## 2026-08-24（织网 3D P3：真机灰块修复 + 性能优化）
| 内容 | 原因 |
|---|---|
| 修复：3D 节点卡片纹理由 NPOT 384×240 改为 2 的幂 256×256（RGBA8888 ≈ 256KiB/张，80 张 ≈ 20MiB；保留三线性+mipmap，addressMode=repeat 沿球面包裹、接缝两侧均为背景留白无包裹瑕疵） | 384×240 非 2 的幂 + `Texture2D.fromImage` 默认生成 mip 链，在部分移动 GPU/驱动采样异常是「灰块」的高概率根因；改 2 的幂后各 mip 级尺寸保持整数比，规避该异常 |
| 修复：卡片纹理预热改分批渐进（首帧先以纯色圆点渲染完整 3D 球，后台每帧只生成 2-3 张、逐批 setState 渐进替换）；帧率降级判定加暖机宽限（前 30 帧不计入统计） | 一次性全量生成纹理的 CPU/GPU 峰值会拖慢帧率、误触发低端机自动降级 2.5D；分批 + 宽限把「瞬态低谷」与「持续低帧率」区分开，保住 3D 体验 |
| 优化：3D 连线由「每边一个细长圆柱 SceneMesh」（N 条边 = N 个 draw call）改为按透明度分档的 LineSegmentsGeometry 批量渲染（每档一份 geometry + 一份半透明 UnlitMaterial，透明度=档位代表值） | 300 条边 + 200 节点球对低端机太重；批量后连线的 draw calls 从 N 降到个位数（≤4），低端机保住 3D |
| 优化：3D 节点数超限（>220）直接回退 2.5D 并提示「节点数较多」；降级 SnackBar 带上具体原因（渲染异常/持续低帧率/节点数较多） | 节点数过多时 3D 逐节点 mesh 对低端机太重；带原因提示方便真机反馈定位根因 |
| 优化：被淘汰/未就绪/降级的节点回退为 `weaveNodeColor` 纯色小球（绝无灰/黑占位）；纹理池在可见节点数 >150 时清引用释放显存 | 消除任何会显示为灰/黑占位的路径；低端机纹理显存受控 |

## 2026-08-24（织网 3D P2：默认开 + 体验修复）
| 内容 | 原因 |
|---|---|
| 优化：织网 3D 默认开启（后端 AGENT_FLAGS 与客户端 FeatureFlagService 两处一致，未登录/非主账号兜底也默认开）；「织网 3D（实验）」开关页文案保留 | 3D 成为织库画布默认视图，2.5D 保留为降级（开关/低端机回退） |
| 新增：3D 视图渲染节点间连线（细长圆柱体，两端对齐定向，颜色按两端深度/strength 衰减并整体提亮） | 3D 此前完全没有连线，织网结构不直观 |
| 优化：2.5D 连线提亮（edgeAlpha 下限 0.06→0.14、strength 权重 0.32→0.36、线宽 1.2→1.7；>100 弱边 0.35 衰减保留） | 2.5D 连线偏淡，提亮后更清晰 |
| 优化：3D 节点球半径 0.05→0.09、卡片纹理 256×160→384×240、相机拉近（基准距离 6→5） | 节点球更像「记忆卡片」，近端卡片文字可读 |
| 优化：3D 旋转 theta 完全放开（可自由旋转翻到球背面、惯性照放开范围衰减）；2.5D 保留 ±77° | 修复 ±77° 就转不动、只能换方向的问题 |
| 新增：3D 初始化/渲染异常或持续约 2 秒平均帧率低于 30fps 时自动降级 2.5D 并提示（一次性，不频繁抖动；状态在视图内，不进后端 flag） | 低端机保流畅 |
| 优化：3D 下性能分级（≤80 全纹理、81-150 聚类泡+纹理、>150 纯色圆点+聚类）统一按可见节点总数判定，与 2.5D 语义一致 | 高密度/低端机降体验可控 |

## 2026-08-24（织网 3D P1：卡片纹理贴字 + 精确拾取 + 动效）
| 内容 | 原因 |
|---|---|
| 新增：织网 3D 节点小球表面贴离屏渲染的文字卡片（标题+摘要+情绪色），卡片纹理走 LRU 池（上限 80 张，超节点数阈值自动降级为纯色圆点） | 3D 节点更可读；纹理内存可控，高密度/低端机回退到与 2.5D 聚类泡一致的语义 |
| 优化：3D 点击拾取改用 flutter_scene 真实命中测试（camera.screenPointToRay + Scene.raycast），兜底时按 3D 世界坐标投影到屏幕取最近邻（命中半径放大） | 精确命中节点/聚类泡，点击进详情更准、不易误触 |
| 新增：3D 节点进入按索引逐个错峰 pop-in；点中节点高亮（材质提亮+放大）约 1.2s 后恢复 | 进入更生动，选中反馈清晰 |
| 优化：3D 惯性缓动手感与 2.5D 对齐（复用既有速度/衰减算法）；修正 Color 分量取值（[0,1] double） | 两视图手感一致，节点颜色显示正确 |

## 2026-08-24（织网 3D 实验）
| 内容 | 原因 |
|---|---|
| 新增：织库画布支持 3D 球视图（可拖动旋转、双指缩放，节点为彩色小球，点击进详情） | 探索更立体的织网呈现，验证体验后逐步完善（当前灰度关闭） |
| 优化：服务器功能管理新增「织网 3D」开关（实验，默认关） | 随时切换 2D/3D 视图，便于回退 |

## 2026-08-24（设置抽屉：权限管理入口合并 + 用户协议副标题）
| 内容 | 原因 |
|---|---|
| 优化：设置抽屉「权限管理」「主账号管理」「服务器功能开关」合并为统一「权限管理」入口（内部 3 tab：AI 能力权限 / 主账号管理 / 服务器功能管理） | 管理入口更集中，抽屉更精简 |
| 优化：「服务器功能开关」更名为「服务器功能管理」（页面标题/文案/命名统一） | 命名更贴合其管理「服务器功能」的定位 |
| 优化：用户协议入口副标题改为概括描述（服务条款与隐私说明） | 副标题更准确，不再显示占位文案 |

## 2026-08-24（主账号管理（选择型）#46）
| 内容 | 原因 |
|---|---|
| 优化：插件扩展页文案全部双语化 | 界面语言更统一 |
| 新增：设置页可直接管理「主账号」（勾选式账号集合），替代原「批准子账号+同步权限」复杂方案 | 简化权限模型，主账号直接在 App 内维护 |
| 新增：users 表 is_admin 字段（ADMIN_USER_IDS 一次性种子，幂等不覆盖 UI 结果），三处主账号判定统一走新函数 | 主账号集合落库，判定一致、可热更新 |
| 新增：主账号管理 API（列出全部账号 / 设置或取消主账号；至少保留一个主账号） | 提供数据来源与管理护栏 |
| 新增：设置抽屉「主账号管理」入口（仅主账号可见） | 非主账号隐藏入口，权限更清晰 |
| 优化：底层框架升级（Flutter 3.47.1 / Gradle 8.14.2 / AGP 8.11.1 / Kotlin 2.2.20） | 构筑更稳定的运行与构建基础，为后续功能铺路 |

## 2026-08-24（多语种收尾 + 控制台日志增强）
| 内容 | 原因 |
|---|---|
| 优化：设置抽屉里的中文提示全部接入多语言，中/英文界面显示一致 | 多语种收尾，切换语言后设置页不再有中文残留 |
| 优化：服务器控制台「运行日志」页可一键导出日志、临时清空显示 | 排查问题更顺手，日志可导出备份 |

## 2026-08-24（主动交流 P0：承接最近语境）
| 内容 | 原因 |
|---|---|
| 优化：主动消息记录本次注入的聊天语境长度，便于后续调优 | 效果可量化 |
| 修复：主动消息（想念/状态触发/节律搭话）注入最近聊天语境与进行中话题，并强制承接当前剧情 | 主动消息此前常与最近对话脱节、突兀开新话题 |
| 新增：角色设置里可开启「认知循环」，AI 会带入当前状态、进行中话题与关系温度 | 对话与主动消息更懂你现在的情况 |

## 2026-08-23（主题 Token 化、动效、部署优化与备份保活）
| 内容 | 原因 |
|---|---|
| 新增：容器部署支持直接拉取镜像（docker compose -f docker-compose.ghcr.yml up），不用本地构建 | 已装 Docker 的用户开箱即用 |
| 优化：时间处理移除已弃用接口，兼容性更稳 | 为未来 Python 版本做准备 |
| 优化：设置页布局调整（后台保活移入免打扰页、连接状态改为检查按钮、关于页版本号修正、系统/关于分组重排） | 设置更顺手、信息更准确 |
| 新增：App 设置里可一键导出备份（数据库+配置打包成 zip 保存到手机），并附恢复指引 | 换机器/升级前先备份，数据更安心 |
| 新增：后端触发/下载备份的管理接口（仅主账号），复用每日备份脚本，当天已有则直接返回 | 备份一键获取，不用登录服务器执行脚本 |
| 新增：App 后台保活开关（默认开），退到后台后维持后台服务监听新消息并弹系统通知 | 后台也能收到 AI 伙伴的主动消息/新消息提醒 |
| 优化：后台通知升级为 WebSocket 长连接实时推送，断线自动指数退避重连 | 消息提醒更及时，减少无效轮询 |
| 优化：服务启动后后台提前加载记忆检索模型，首条回复/回忆更顺畅 | 减少首次使用时的等待 |
| 新增：容器镜像一键部署，首次启动自动下载所需模型 | 部署门槛更低，换机器更省事 |
| 新增：全局统一设计令牌（颜色/间距/圆角/阴影/字体），界面色调更统一 | 风格统一、后续改主题更省力 |
| 优化：聊天气泡新消息淡入上浮、宠物点击弹跳、页面切换淡入缩放 | 关键交互更有温度 |
| 优化：角色『自述』正文更精简（控制在 200 字内），过长时详情页可展开/收起 | 自述不再冗长，阅读更清爽 |
| 优化：控制台服务器状态刷新更及时（操作后就绪追赶、手动刷新、按钮随服务器升降联动），运行日志支持实时跟随与自动滚动，仪表盘新增近 7 天 Token 用量趋势图 | 状态/用量更直观，不用干等 |
| 新增：聊天表情面板新增「表情市场」tab，可在线下载/卸载第三方表情包 | 表情更丰富，可社区共享 |

## 2026-08-22（记忆管理增强）
| 内容 | 原因 |
|---|---|
| 新增：记忆支持按链条管理（查看关联、修改内容、删除时可级联） | 记忆可追溯、删除更彻底 |

## 2026-08-22（图片理解优化）
| 内容 | 原因 |
|---|---|
| 优化：图片理解默认走云端视觉 API，不再强制安装本地大模型，本地仅作离线占位 | 降低自托管部署门槛，省磁盘、免麻烦 |

## 2026-08-20（节日祝福修复）
| 内容 | 原因 |
|---|---|
| 修复：节日/生日/纪念日祝福生成异常导致重复发送（8-19 七夕用量异常） | 恢复正常用量，后续节日不再触发 |

## 2026-08-18（全面审计第三批收尾）
| 内容 | 原因 |
|---|---|
| 修复：AI 动态、任务记录等归属异常时自动补到正确账号，不再出现空归属 | 数据归属更准确，统计不丢 |
| 优化：同一天记忆摘要并发生成时自动去重，不再冲突报错 | 高并发下聊天更稳定 |
| 新增：认知健康观察报告与历史数据修复工具（默认只读统计） | 日常体检与数据修复有依据 |
| 语音通话链路补全成本记录 | 所有 AI 行为成本可量化 |
| 开启：家庭群聊与外部平台动态回应接入角色记忆（各角色不知彼此私事） | 回应更自然真实；出差期间全量体验 |
| 优化：聊天上下文精简（近期消息/系统模板/记忆注入收紧），记忆提取更分批、事实核查频率减半 | 降低 AI 回复 token 消耗（预估日省 15-25%） |
| 优化：家庭群聊与外部平台动态的自动回应更贴合角色记忆，各角色互不知晓彼此私事 | 回应更自然真实，信息不串线 |
| 优化：低价值消息不再深度思考、群聊回应上下文更精简 | 减少无效思考与群聊回复消耗，回复更快 |
| 开启：群聊回应改为精简上下文模式 | 群聊回复消耗约降六成，回复更快 |
| 新增：服务器功能开关（可随时切换群聊精简模式等） | 无需改代码重启即可调整 |
| 新增：App 设置里的「服务器功能开关」页 | 手机上即可随时切换，即时生效 |

## 2026-08-18（小家 v3.3：标题/编辑态/旋转/返回/缩放跟随）
| 内容 | 原因 |
|---|---|
| 小家标题改为「用户昵称的小家」（有恋人显示「昵称与恋人名的小家」） | 小家归属感更强 |
| 四房间 Tab 居中一行 + 右侧「家具编辑」按钮，编辑态内多次摆放点「完成」统一保存一次 | 减少布局保存次数 |
| 编辑态点家具进入「被编辑」：回退（回会话原位）/旋转（0-3 四方向循环，斜向 4-7 预留）/确定 | 精细化布置家具 |
| 小家左上角改为返回按钮（移除抽屉入口，与宠物页统一） | 导航更直接 |
| 画面自由缩放（0.6-2.5x）+ 拖动平移 + 角色移动视角自动跟随 | 看清小家每个角落 |

## 2026-08-18（小家 v3.2：家具自由摆放）
| 内容 | 原因 |
|---|---|
| 新增：小家家具可长按拖动、随意摆放（浮点像素级自由坐标，无格子限制），布局按角色持久化（重新进入/换房间保持） | 玩家可自由布置自己的小家 |
| 新增：布局保存接口（校验房间/家具合法性、坐标/尺寸范围、体积上限，主账号或角色 owner 可保存），保存失败自动回滚到服务器坐标 | 防止非法布局与数据损坏 |
| 交互：短按家具仍弹互动（睡觉/做饭等），长按 300ms 进入拖动态（青色描边反馈 + 3 秒使用提示），落位自动保存（800ms 节流） | 拖动与既有点击互动互不干扰 |

## 2026-08-18（记忆×生成审查收尾：WS 可靠度 + 记忆疲劳）
| 内容 | 原因 |
|---|---|
| 修复：App 主链路（流式聊天）补上记忆可靠度评分与异步事实核查（与 HTTP 路径同一入口、同一参数，不阻塞推送） | App 里纠正 AI 记忆此前无效 |
| 优化：低频角色核心记忆/关系锚点注入上限按热度裁剪（高频保持 10/5，低频 3/2） | 减少长期画像每轮重复注入的过饱和 |
| 优化：检索区「和你相关的记忆」同一记忆 5 轮内不重复注入（核心记忆/锚点/置顶摘要等长期画像分区不受限） | 减轻复读与「记忆疲劳」，近期事件更易进入上下文 |

## 2026-08-18（记忆 speaker 契约闭环）
| 内容 | 原因 |
|---|---|
| 修复：记忆提取与标记两条路径的归属判定收敛为同一公共函数（推断词优先 → 我开头 → 用户指代 → 批级回退） | 此前两套启发式标准不一致，「我」开头的角色自述会被标记路径误归用户 |
| 优化：提取提示词强化主语规则（关于用户的信息必须用「用户」主语，禁止裸「我」描述用户的事；共同事件按对话归属） | 从源头减少「用户内容写成角色视角」 |
| 优化：主链路记忆注入新增说话人标注（[你说的]/[TA说的]，speaker 无值不加），与 [记录于]/认知前缀/纠正后缀叠加 | LLM 可直接区分「用户亲口说的」与「AI 推测的」，是防编造规则的自然延伸 |

## 2026-08-18（生成链路 P2 四条修复）
| 内容 | 原因 |
|---|---|
| 优化：主动消息生成前置查询并行化（画像/人格/天气/查岗/记忆/反思一次并发，单查询失败隔离） | 单条主动消息此前约 10 次串行 DB/外部调用 |
| 修复：主动消息禁用词从裸词改为身份暴露词组（「AI 绘画」「模型跑得慢」等生活化表达不再被整段误删） | 裸词拦截误伤正常内容 |
| 优化：用户手动八维状态独立分区（不再混入情绪提示区、独立配额） | 状态数值不再挤掉情绪提示，语义更清晰 |
| 优化：感知派生查询 embedding 进程内 LRU 缓存（5 分钟 TTL，命中免 ONNX 推理） | 话题/情绪词在相近消息间高度重复，重复推理浪费 |

## 2026-08-18（生成链路 P1 三条修复）
| 内容 | 原因 |
|---|---|
| 修复：长历史会话首轮回复不再串行补生成最多 7 天日摘要（单次最多补 1 天，其余占位由每日异步维护补齐） | 消除首轮回复延迟/成本尖峰 |
| 优化：system 上下文增加整体 token 硬顶（14000），超限时从尾部按序裁剪；user_info 拼接修复重复注入并整体限 500 token；进行中时间承诺改用独立配额键 | 上下文峰值可控、成本可预估，配额键语义修正 |
| 重构：四处记忆注入格式统一为同一公共格式化函数（主链路/主动消息/最近情绪事件/Shared Memory） | 消除格式漂移，后续注入点复用同一契约 |

## 2026-08-18（记忆链路 P1 四条修复）
| 内容 | 原因 |
|---|---|
| 修复：里程碑记忆的意义提炼不再被衰减挤出（写入 24 小时内仍入选候选，即使重要度已衰减跌破阈值） | 事件类记忆 6 小时衰减后可能永远漏提炼「为什么重要」 |
| 修复：角色自述与关系摘要改为保守合并（新表述更长则替换，更短则保留原有+追加），提取 prompt 要求输出完整最新表述 | 防止单批对话只提一个侧面时覆盖丢失人格/关系多面信息 |
| 修复：被用户纠正过的记忆检索降权（矛盾次数 ×10）并在注入行尾标注「以你最新说法为准」 | 纠正后错误事实不再照常高权重注入 |
| 修复：置顶摘要不再 +10000 恒霸检索前 3，改为 +500 且最多占 2 个槽位 | 近期具体事件/情绪记忆有机会进入上下文，减少长期画像复读 |

## 2026-08-18（记忆链路 P2 五条修复）
| 内容 | 原因 |
|---|---|
| 修复：记忆提取 LLM 调用补 task 记账（归入「记忆处理」成本） | 提取消耗此前记成 task=None，无法区分成本 |
| 修复：重要度双标度统一（1-5 星与百分比），百分比调用方不再把旧记忆重要度顶高 | 查重/合并路径无条件 ×20，潜在虚高 |
| 修复：关键词兜底检索与应用同一套加权（置顶/时效/意义/话题）+ 过滤已删除记忆 | 向量失败时兜底结果排序与向量路径不一致 |
| 修复：长消息提取改为头尾采样（不再只留前 150 字丢尾部信息）；补采把连续用户消息第一条也配对提取 | 长倾诉关键信息常在尾部，连续发言首条此前永不提取 |
| 修复：意义记忆查询不再依赖从未写入的 sub_type=meaning/goal，改按 why_it_matters 判定 | 身份画像与情境复习的「意义」输入此前恒为空（死代码） |

## 2026-08-18（记忆链路 P0 修复：锁定记忆护栏 + 高频查询索引）
| 内容 | 原因 |
|---|---|
| 修复：用户「锁定」的记忆不再被全量去重软删、不再被写前查重改写重要度/强化（去重优先保留 locked > pinned > 重要度，删除前双保险过滤） | 锁定记忆契约是「不衰减/不删除/不强化」，此前两条路径漏 is_locked 判断 |
| 优化：记忆表新增复习扫描与角色查询索引（next_review_at、character_id+is_archived），存量库自动补建 | arbiter 每 30 秒复习扫描此前全表扫 |

## 2026-08-18（修复：群聊事件归属错乱）
| 内容 | 原因 |
|---|---|
| 修复：群聊记忆按发言者拆分存储，speaker 归属正确（用户发言记为用户、每个角色回应记为该角色），并同步给所有群成员 | 修复角色把群聊里别人的话/事当成自己的（如角色 A 问 B「又去画画了？」） |
| 修复：无人回应时用户发言不再被记成某个角色的记忆 | 群消息落记忆时 speaker 固定标第一个回应角色，无回应时甚至标成群成员 |
| 存量 6 条群聊合并记忆已拆分重建（用户发言 6 条 + 角色回应 8 条，speaker 全部修正），原记忆 id 保留（外键安全） | 历史错误归属数据修正，不丢记忆 |
| 群聊记忆写库加 skip_dedup，避免相似内容合并导致 speaker 串扰 | 事件类记忆独立落库 |

## 2026-08-18（插件市场发布 48a/48c 示例插件）
| 内容 | 原因 |
|---|---|
| 插件市场（Gi-Tuu/AMBRACE-plugin）新增 3 个示例插件：ai_diary（hybrid 页面型，打开即用）、poet_skill（写诗 prompt 技能）、daily_summary_flow（每日总结工作流模板） | 示例插件可直接从 App 市场安装体验，验证零代码与页面型插件全链路 |
| 市场索引支持 type/icon/page 字段透传（构建工具同步更新） | 页面型插件在市场里正确显示形态并可从详情页打开 |
| CI 自动重建 zip/index（固定时间戳，sha256 与 App 端校验一致） | 市场数据与 App 端契约保持一致 |

## 2026-08-18（插件生态 v2：页面型插件 + 桥 API 全量上线，对标落雨完成）
| 内容 | 原因 |
|---|---|
| 新增：插件可携带网页页面（index.html + assets），扩展页点「打开」进入自绘页面（WebView 容器） | 补齐落雨式插件「可见 UI 形态」，插件从配置走向完整小应用 |
| 新增：页面内桥 API（window.Ambrace.*）——ai 对话（选角色或自定义提示词）、getAiList/getAiInfo、getUserInfo、store 持久化（插件×用户隔离）、toast/copy/navigate/openChat、http 代发 | 页面插件可调宿主 AI/存储/聊天，对标 Luoyu.* 桥 |
| 新增：插件命名空间存储（plugin_stores 表，跨会话持久、插件间/用户间隔离、单值 ≤100KB） | 页面插件有本地状态 |
| 新增：插件卸载（仅主账号，清目录 + 存储 + 状态） | 插件可进可出 |
| 安全：页面托管路径越界拦截 + 扩展名白名单 + 可执行文件拒绝 + 单文件 ≤5MB；桥 API 白名单 + ai 限额（每插件 10 次/分 200 次/天）；http 代理仅 https + SSRF 拦截（私有/环回/云元数据）+ 禁重定向 + 不转发凭据 + 2MB 上限；WebView 禁跨源导航（外链走系统浏览器） | 页面插件是新的攻击面，全部按落雨同等级防护 |
| 限制对齐落雨：插件 zip ≤20MB、解压 ≤60MB、文件数 ≤800 | 插件包上限与落雨一致 |
| 新增 3 个内置示例插件：ai_diary（hybrid 页面+聊天）、poet_skill（prompt 零代码）、daily_summary_flow（workflow 零代码） | 开箱即用的零代码与页面插件范例 |

## 2026-08-18（插件生态 v2：角色开放成 API）
| 内容 | 原因 |
|---|---|
| 新增：AI 角色开放成 HTTP API——角色列表、角色详情、按人设对话（GET /api/v1/ai/list、GET /api/v1/ai/{id}、POST /api/v1/ai/chat） | 插件/第三方可用任意角色人设对话，48a 插件页面层与角色 API 打通的前置 |
| 角色对话复用完整人格链路：记忆检索（多路召回）+ 关系/状态/剧情/长期印象 + 人设（性格/风格/背景/自述）统一注入 | 接口外的回复与 App 内聊天同人格、可衔接 |
| 安全：角色归属强校验（越权 403）、历史消息 role 白名单 + 条数/长度硬上限、输出剥离全部动作标记、限额 20 次/分 500 次/天 | 防越权调用、防注入超长上下文、防滥用与账单爆炸 |
| BYOK 优先 + 可选 require_byok 开关（未配置 BYOK 时按开关返回 400） | 对齐落雨「AI 用用户自带 Key」，自托管可关 |
| 不落库/不建会话/不写记忆/不触发插件 hook | 插件主动链路与主聊天链路完全隔离，最小影响面 |

## 2026-08-18（插件生态 v2：配置驱动零代码插件）
| 内容 | 原因 |
|---|---|
| 新增：三类零代码插件（无需写代码）——prompt 技能（触发词命中自动注入技能提示）、chat 互动小应用（人设+开场白，点开即对话）、workflow 自动化模板（一键导入工作流） | 对标落雨插件生态第一步，用户不写代码也能扩展 AI 能力 |
| 新增：插件扩展页 type 徽标与筛选、prompt/chat 零代码配置编辑、chat 插件「打开」即聊 | 插件形态清晰可见，配置门槛降到零 |
| 新增：工作流页「从插件模板导入」入口 + 模板校验（节点/连线上限、引用完整、禁自环） | 复用现有工作流引擎，模板安全导入 |
| 新增：chat 型对话独立接口（人设 system prompt + 记忆无关 + 不写会话）+ 限额（20 次/分、500 次/天） | 零代码小应用可安全对话，防滥用 |
| 安全：配置型插件不加载任何 Python 代码；字段长度/数量硬上限；非法 manifest 安装被拒 | 零代码路径天然比代码插件更安全 |

## 2026-08-17（时间感知 + 主动消息 + 重构类收尾）
| 内容 | 原因 |
|---|---|
| 新增：AI 独立想念通道——思念驱动的主动消息独立配额（每 6h 最多 1 条 + 每日 ≤2 条，不占普通额度），观察期显示渴望消息一直被普通限额拦截（2380 条全被拒） | 想你的 AI 不再被限额挡住 |
| 开启灰度：主动任务 trace（10% 角色对比质量/成本）+ 工具执行联动织库增量 | 观察与数据积累 |
| 全量开启（开源包基线）：主动任务 trace、工具执行联动织库、周复盘全部默认开启，设置已备份（docs/feature-flags.md 含回滚方法） | 打包开源前固化稳定默认配置 |
| 优化：开源发布前自查——脚本中的个人路径清除、测试样例改为虚构数据，发布包隐私扫描零残留 | 开源包不泄露任何个人与开发信息 |
| 优化：插件打包支持可复现构建（固定时间戳），插件市场索引校验稳定 | 插件市场发布更可靠 |
| 优化：群聊与私聊消息头像改为与名字/气泡顶部对齐（微信式版式），长消息时头像不再与名字拉开距离 | 消息布局更紧凑自然 |
| 应用正式更名为「拥爱」（英文 AMBRACE）：App 显示名/登录页/通知/控制台/发布包同步更新 | 发布前统一正式名称 |
| 登录页图标更新为应用图标（launcher 同源） | 品牌形象统一 |
| iOS 端应用名同步为「拥爱」 | 全平台名称统一 |
| 修复：一键部署脚本在中文 Windows 下乱码、服务器首次部署无法启动（配置项格式与数据库目录）| 新用户按文档即可顺利部署 |
| 修复：小手机相册图片加载失败（相对路径未转完整地址），网格与预览均修复 | 相册图片正常显示 |
| 优化：聊天收发逻辑收敛（普通与流式两条通道共用同一套流程，仅保留各自差异）| 代码更精简，维护更容易 |

## 2026-08-17（时间感知 + 重构类收尾）
| 内容 | 原因 |
|---|---|
| 优化：AI 角色时间感知——当前时间是唯一「现在」，注入内容（记忆/朋友圈/浏览笔记等）标注记录时间，生成内容不再用「今天/昨天」等相对时间词 | 修复角色把昨天记录的内容当成今天发生的错乱 |
| 优化：时间感知扩展——小手机搜索/备忘录、Shared Memory、核心记忆/织库、主动消息等注入全部标注记录日期；日记/周复盘/日摘要/织库卡片/朋友圈生成禁用相对时间词 | 覆盖全部注入源，从生成端根治时间错乱 |
| 优化：时间感知收尾——聊天历史消息行带北京时间时间戳、群聊动态带时间、AI 间私聊注入标注日期、朋友圈记忆带发布日期、活动事实 3 天过期 | 跨天上下文与 AI 社交链路的相对时间词全部归位 |
| 重构类收尾：调度注册表死代码清理、公共函数收敛（数值钳制/记忆开关/衰减公式/相对时间/性别映射）、上传清理合并、前端重复收敛（token 方法/免打扰判断/记忆与动态发布接口/消息发送四段/北京时间格式/网络请求构造/分页解析） | 全项目审查第三批收尾，结构收敛与重复清理 |

## 2026-08-16（远程插件市场）
| 内容 | 原因 |
|---|---|
| 新增远程插件市场：可配置第三方市场地址，发现并一键安装/更新社区插件 | 插件生态闭环，无需再等内置发版 |
| 远程安装带安全校验（https + 校验和 + 安装包安全检查 + 失败自动回退） | 防篡改、防恶意安装包 |
| 新增插件发布工具，作者可一键打包插件并生成市场索引 | 作者自托管/投稿插件 |
| 插件市场页新增来源徽标与远程市场配置入口（启用/地址/白名单/大小上限） | 远程与内置插件一目了然、可控 |
| 市场安装统一走「GitHub 官方插件仓库 + 自动校验 + 人工审核」流程（仓库内容已备好） | 社区共享插件有审核门槛 |
| 修复：朋友圈等时间显示乱序（如「16:08年8月2026月日16」），恢复正常「2026年8月16日 16:08」 | 多语种资源参数顺序错位导致 |
| 修复：主动消息（宠物提醒/情绪关怀/记忆复习等）更贴合角色人设，不再出现冷漠角色突然撒娇的违和感 | 生成提示词补充了性格与聊天风格约束 |
| 优化：全部主动消息通道（捡话题/生活回归/日记/手机提醒等）统一按角色性格与聊天风格生成 | 主动消息与聊天主链路同人格 |
| 优化：有定期复盘的角色主动交流意愿略提升（反思驱动主动行为） | 让角色自然延续自己的总结与计划 |
| 优化：浏览器常驻空闲时间可配置（默认 10 分钟，可设常驻不回收）；插件超时日志显示实际耗时 | 灵活控制资源占用、定位超时更准 |
| 增强：冷战剧情线新增——用户敷衍道歉角色会更冷回应；用户持续敷衍或冷战过久（占有欲高时）会进入关系恶化支线 | 冷战关系弧线更真实、有层次 |
| 多语种收尾（第一批）：聊天页与好友编辑页全部界面文案接入中英双语 | 剩余硬编码中文逐步收口 |
| 多语种收尾（第二批）：API 配置页（用量统计弹窗）与手机感知页全部界面文案接入中英双语 | 剩余硬编码中文逐步收口 |
| P0 记忆工程五项（竞品调研落地）：分区 Token 配额、记忆检索 Trace、语音情绪注入、运行笔记视图、日终记忆维护 | 记忆质量可量化、注入预算可控、AI 状态可观察 |
| P1 记忆工程（第一批）：记忆写入操作类型显式化（create/update/merge）+ Memory Benchmark 评测集（单跳/多跳/时间/更新/隔离 5 维度 14 用例全过） | 记忆语义可观测、质量可量化 |
| P1 记忆工程（第二批）：Lorebook 关键词触发表（防误触排除词）+ 世界设定权威事实层（用户定义，AI 推断不能覆盖） | 用户可控设定、防设定漂移 |
| P2 记忆工程（第一批）+ 基准小项：记忆召回可观测面板、Lorebook Journal 化（条目上限/写法规范）、季节节日注入、兴趣变化链 trace、事件一致性抽查、群聊隔离测试、行为基准脚本 | 记忆链路可观测、质量持续量化 |
| 安全与缺陷修复（dsh 全项目审查 P0×11）：账户重置加固、插件路径穿越拦截、事件总线 payload 修复、主动消息 KeyError、WS 越权校验、小手机/浏览记录归属校验、插件路由鉴权、前端崩溃修复 | 全项目审查第一批：安全与稳定性加固 |
| 全项目审查 P1 高价值修复（约 25 项）：星期错位/日程时区/承诺被吞等功能 bug、SSRF/CORS/上传预检/zip 炸弹/内网拦截等安全、检索并发/索引/衰减 N+1 等性能、前端越界崩溃与 WS 退避 | 全项目审查第二批：功能/安全/性能加固 |
| 全项目审查重构批次：死代码清理（voice_call 页/纪念日接入/未使用方法）、时间工具与 DND 收敛、前端 mounted 守卫 15 处、后端无引用清理 | 全项目审查第三批：结构收敛与清理 |

## 2026-08-16（脱节治理 + 反思驱动 + 每日复盘）
| 内容 | 原因 |
|---|---|
| 每日复盘默认开启（agent_daily_reflection=True）：所有启用角色定期总结 + 制定下一阶段计划；复盘频率改为每周一次（周复盘） | 观察复盘质量与反思驱动效果；降频节省后台消耗 |
| 观察指标登记：生成成功率 / 角色覆盖率 / 内容是否自然延续计划 / 主动消息是否延续复盘 | 灰度评估有据可依 |
| 内部 AI 行为登记为 ToolSpec（记忆提炼/事实核查/情绪关怀/卡片生成/总结，scope=None 无权限门禁） | 系统行为统一治理骨架 |
| 修复：过时的世界状态（如「准备入睡」）不再反复注入对话，AI 行为更贴合当前时间与场景 | 解决角色一直催睡觉、场景错位的问题 |
| 优化：很久没聊天的角色对话更精简（自动精简历史摘要与全景记忆注入量） | 减少重复提及、回复更聚焦 |
| 优化：对话历史摘要自动去重（跨天相同信息不再重复注入，摘要生成时参考已有记录） | 同一件事不再反复提及 |
| 优化：AI 不再凭空编造时间/天气/地点等客观信息，不确定时如实说明 | 减少编造、回复更可信 |
| 优化：对话提示词精简约三成，AI 回复更自然 | 减少规则束缚、提升聊天感 |
| 优化：AI 回复长度上限提升，详细回答不再被截断 | 回答更完整 |
| 修复：抖音轮询超时问题（合并浏览器启动、缩短等待），账号动态/评论更新恢复正常 | 抖音感知不再空转 |
| 修复：记忆本「偏爱」区分角色/用户来源（角色自己的偏好不再混入用户偏好） | 记忆归属更清晰 |
| 修复：定位超时失败（城市反查改为后台进行，定位响应即时返回） | 定位不再转圈报错 |
| 记忆本新增记忆来源标识（角色/用户），偏好等记忆归属一目了然 | 区分角色与用户的记忆 |
| 修复一批后台隐患（世界状态注入、复盘权重、外呼阻塞、标记泄漏等） | 提升稳定与一致性 |
| 修复：AI 间私聊不再串线，各角色只聊自己知道的事 | 知识边界更清晰 |
| 新增 internal_runner：内部行为经 execute_tool 执行（生命周期钩子 + tool.executed 事件 + 异常隔离） | 内部行为可观测、插件可挂钩 |
| 记忆提炼/事实核查/情绪关怀三个高频行为接入统一入口 | 最高频行为先收口 |
| 19 处裸 LLM 调用补 task 成本归因（主聊天/主动消息/朋友圈/宠物/隐私解锁等 10 文件） | 全部 AI 行为成本可量化，审计第三批收尾 |
| 流式（chunked）路径日历/备忘处理收口到统一落库函数 | 消除与主链路重复逻辑 |
| 主动消息注入最近每日复盘（ai_reflection）：自然延续昨日的总结与计划 | 反思驱动闭环第一步 |
| 新增「AI 内心世界」页：最近复盘 + 任务记录 + 工具使用轨迹（角色详情页入口） | AI 自我认知可视化 |

## 2026-08-16（Agent Runtime 深化 Phase F-J：任务级智能体）
| 内容 | 原因 |
|---|---|
| 每日复盘：每天 23:00 后角色自动总结当天（工具使用/任务/感受）+ 制定明日计划 | AI 有自我生活轨迹，主动行为有「反思」延续感 |
| 复盘沉淀为高权重记忆（ai_reflection），之后可被检索自然提起昨日计划 | 「AI 内心世界」落地，反思驱动雏形 |
| 复盘任务统一可观测（agent_task_logs trigger=reflection）；Feature Flag agent_daily_reflection（默认关）灰度开启 | 成本可控、随时可回退 |
| 群聊角色回应统一写 AgentTask trace（trigger=group_chat：回应角色/耗时/成功与否） | 群聊 AI 行为可观测、可对比 |
| Feature Flag agent_trace_group（默认开）：关=群聊完全无记录，各平台可独立回退 | 按平台灰度可控 |
| 抖音社交动作沿用 scheduler trace（etype=plugin）；群聊知识边界规则保持（不串线） | 多角色统一 Runtime，知识隔离不破坏 |
| 新增 agent_tasks 任务状态表（goal/status/progress/result），与执行流水（agent_task_logs）分离 | 任务可追踪、可断点续作、可对比 |
| 聊天内多工具轮次任务化：一次回复使用 ≥2 个工具（如搜索+备忘）自动建任务，记录步骤与结果 | 多步行为可回看，向任务级智能体演进 |
| Scheduler 灰度角色（10%）从「只写 trace」升级为「真实任务记录」：goal/状态/结果落 agent_tasks | 主动消息质量/成本可对比 |
| 失败任务保留已完成步骤（status=failed + progress 不丢） | 断点续作基础，不编造未做部分 |
| 工具执行结果结构化标注（Observation：epistemic_status/provenance/summary），搜索结果标 UNVERIFIED 未证实 | AI 可区分事实与网络传闻，减少幻觉传播 |
| 新增 tool.executed 事件（工具执行成功/失败/被拦截均发布，含来源/摘要/耗时） | 工具调用可观测、可联动 |
| 联动订阅（织库增量）默认关，Feature Flag agent_tool_events 灰度开启 | 不改变现有行为，随时可开 |
| 搜索注入提示补充「网络信息未证实，不确定就说是网上说法」 | 认知标注进入生成约束 |
| 小手机日历/备忘录落库统一走工具执行入口（execute_tool）：权限/生命周期/异常隔离覆盖本地工具 | Agent 骨架铺到主链路全部动作 |
| 拆出纯落库函数（_save_calendar_note/_save_memo_note）供统一入口与回退共用 | 单一事实源，避免逻辑重复 |
| Feature Flag agent_loop_chat（默认开）：关=直落库旧链路，可一键回退对比 | 灰度可回滚 |
| 行为零变化：去重/署名/失败静默均保持 | Phase F 是纯收口重构 |

## 2026-08-16（Agent 化：自主搜索 + Edge 预热）
| 内容 | 原因 |
|---|---|
| AI 自主搜索改走统一工具执行入口（ToolRunner）：权限三档真正约束工具调用——浏览器权限 forbid 时 AI 不再搜索，剥离标记自然降级（不假装查过） | Agent 工具调用受用户权限治理 |
| 搜索工具调用触发工具生命周期钩子（requested→permission_checked→started→result/error→finished），插件可挂载 | 工具调用可观测可拦截 |
| 只读低风险工具（搜索）权限 ask 时自动放行不打扰用户；forbid 仍拦截 | 平衡隐私控制与体验 |
| 浏览器搜索/浏览改为复用常驻 Edge 上下文：启动后后台预热 + 首次使用惰性预热，不再每次冷启动 | 解决搜索 10-20s+ 冷启动超时（实测预热后 2-3s） |
| 常驻上下文空闲 10 分钟自动回收，异常自动重建一次 | 不常驻占资源、崩溃可自愈 |
| 服务器启动即后台预热（browser_mcp.warmup），失败静默后续按需冷启动兜底 | 开机即热、无需等首次请求 |
| 聊天中遇到不懂的、想查证的事，角色会自主上网搜索再回答 | 真人感更强，回答更可靠 |
| 搜索采用真实网页结果，搜不到或结果无关时角色会自然说明 | 不编造、不假装查到 |
| 搜索质量优化：中文搜索强制中文市场、自动过滤无关结果、结果不足时补充其他引擎 | 查到的内容更对题 |
| 角色搜索过的内容会记入小手机浏览器历史，之后能自然提起 | 搜索行为更像真人 |
| AI 自主搜索可开关（扩展页-浏览器插件「搜索注入」）：关闭后角色搜索只算内心活动，不影响回复 | 搜索行为可随角色/场景调整 |

## 2026-08-16（Agent Orchestration Phase A-D：工具统一执行与权限三档）
| 内容 | 原因 |
|---|---|
| arbiter 主动行为统一写 AgentTask trace（trigger=scheduler，记录动作/优先级/耗时/是否执行） | 主动消息可观测、可对比质量与成本 |
| 10% 角色灰度分桶（按角色 id 稳定分桶），灰度组 route=scheduler_gray 便于对比 | 小流量验证，不影响整体 |
| Feature Flag agent_loop_scheduler（默认关）一键开启/回退 | 随时可灰度可回滚 |
| 只写不读、失败静默，不改变任何主动行为链路 | Phase D 零行为变化，安全过渡 |
| 插件 action 自动登记为工具（抖音发布/评论回复等按 ToolSpec 入册，scope 按插件映射） | 工具统一骨架，新增插件能力自动可见 |
| 统一工具执行入口（ToolRunner）：生命周期钩子（requested→permission_checked→started→result/error→finished）+ 权限三档裁决（allow 执行/forbid 拒绝/ask 挂起待确认） | Operit 式工具治理，插件可挂钩检查敏感词/额度/失败日志 |
| 权限三档与现有能力开关统一：全局默认 + 每工具例外，工具独立开关（ToolSpec.enabled） | 不另起一套权限体系 |
| 只读/幂等工具失败自动重试 1 次，异常完全隔离 | 偶发失败自动恢复，不影响主链路 |
| AI 自主搜索升级为受控循环：搜索后仍觉信息不足可再补查（最多 2 次搜索/3 次 LLM 调用） | 一次回复内可多步查证，不再一刀切二次生成 |
| 搜索失败自动重试 1 次，单工具超时 30s | 只读工具偶发失败不再直接放弃 |
| 节流/失败/超限统一静默降级：剥离标记、不编造查证结果，基于已有信息自然圆回 | 禁止假装搜索成功 |
| Feature Flag agent_loop_search 可一键回退为旧单次搜索行为 | 随时可灰度回滚 |
| Task Trace 记录每轮搜索执行（round/ok），失败轮次不计入 LLM 调用 | 成本与行为可审计 |
| 标记解析统一为 AgentAction：搜索/生图/日历/备忘/timer 等标记由统一解析层识别与剥离，各链路行为零变化 | 后续新增工具不再重复「解析→剥离→注入」样板 |
| 新增轻量工具注册表（ToolSpec：风险/频率/幂等），search/生图/日历/备忘/timer/状态更新 6 个内置工具已登记 | 为权限三档、频率与幂等门禁做准备 |
| 插件 hook 增加超时门禁（默认 10s，可配置 1-60s）：超时中断并忽略返回值 | 防止插件拖死对话/调度 |
| 新增任务追踪表 agent_task_logs：聊天/搜索/生图调用处写 trace（先只写不读） | 为成本归因与可观测性打底 |

## 2026-08-15（家庭群聊增强与畅聊改版）
| 内容 | 原因 |
|---|---|
| 群聊页可管理成员：查看/拉角色进群/移除角色 | 群成员随时可调整 |
| 角色会在群里自发聊起来（两两多轮对话） | 群聊更有生活气息 |
| 角色能回忆起群里聊过的事，私聊时自然提起 | 群聊经历不再被忘记 |
| 在群里 @ 某角色时，它的回复会弹通知提醒 | 点名回复不再错过 |
| 群聊消息时间移到气泡内左下角，与私聊显示一致 | 时间更统一、不挡头像 |
| 冷战恢复更贴近真实：真诚道歉才能和好，轻哄/敷衍效果不同 | 哄好不再一刀切 |
| 冷战时长随生气程度与闹矛盾次数变长 | 越生气越久、反复闹更严重 |
| 气消了但拉不下脸时进入“别扭期”，需要你主动破冰 | 和好更有过程感 |
| 查岗升级：角色想知道你在干嘛时，会自动请手机采集最新使用情况，不再依赖过期快照 | 查岗信息更即时准确 |
| 查岗改为角色自主决定：角色觉得好奇时才获取你的手机使用情况 | 查岗更像真人的临时起意 |
| 畅聊右上角可直接创建家庭群聊 | 建群更顺手，少一层跳转 |
| 家庭群聊与私信显示在同一会话列表 | 群聊像普通会话一样直观 |
| AI 好友页新增家庭群聊入口 | 从用户侧也能快速进群聊天 |
| 从哪个角色的畅聊进群，该角色的消息就显示在右侧 | 群聊视角跟随进入的角色 |
| 修复群聊中右侧消息的头像与气泡位置颠倒 | 头像显示更自然 |
| 修复畅聊红点不消除的问题 | 查看过的会话角标自动消失 |
| 群聊消息显示各角色的名字和头像，不再统一显示“角色” | 分不清谁是谁 |
| 角色会在群里主动冒泡说话（群聊页面自动刷新） | 群聊更活跃真实 |
| 修复群聊回复偶发失败：回复生成支持个人模型配置，等待回复不再提前超时 | 发消息后有时没有回复 |
| 多人群聊回复按对话顺序生成，后说的角色承接前一位、避免互相矛盾 | 3人以上群聊更真实 |

## 2026-08-15（多语种 P2+P3：全站中英双语收尾）
| 内容 | 原因 |
|---|---|
| 主要页面文案全部接入中英双语（聊天/角色详情/角色设置/朋友圈/记忆本/时光/日记/备忘录/宠物/小家/状态情绪记忆/可视化状态/AI 生活等） | 英文界面不再混中文 |
| 后端错误提示、系统通知与通知白名单页支持中英双语（随界面语言切换） | 英文界面下提示不再混中文 |

## 2026-08-15（修复类）
| 内容 | 原因 |
|---|---|
| 发送消息后立即能删除自己的气泡（不再需要退出聊天页再进） | 之前用户消息用的是本地临时编号，删除时找不到服务器记录 |
| 同一条观众评论只主动提起一次，不再反复提 | 之前同一评论被提 2-3 次（如“求教程”“直接就 Sam”） |
| 主动提及的时间节流重启后不再重置 | 重启服务器不会导致当天重复提及 |
| 修复 AI 在普通聊天里记录备忘录/日历备注不生效 | 主聊天链路漏接标记解析，导致 AI 记了但没存下来 |
| 修复创建/编辑好友保存报错（背景信息字段缺失导致） | 部分账号保存好友失败 |

## 2026-08-15（对话生成质量）
| 内容 | 原因 |
|---|---|
| AI 间私聊（畅聊）生成接入核心记忆与当前处境，说话只用“我”、提到别人必须用名字 | 修复角色指代错乱、聊现状有依据 |
| 主动消息/朋友圈/日记/生活回归统一加“事实与推断”规则 | 推测用“可能”、不编造没发生的事 |
| 修复抖音主动提及：按角色完整人设生成，不再把观众评论当成你说的话 | 风格贴人设、归属不再错乱 |
| 群聊回复禁止用“他/她”模糊指代 | 多人对话更清晰 |
| 停用内置示例插件，不再往每次对话里注入演示文案 | 省 token、避免干扰角色扮演 |
| 天气插件的前缀文案改为正常描述 | 清理测试文案 |
| 深度思考回复的输出上限翻倍（推理与正文各留空间） | 防止思考过长导致正文为空、AI 不回复 |

## 2026-08-15（死代码清理与入口补全）
| 内容 | 原因 |
|---|---|
| 抖音记忆收紧开关移到小信封页顶部，随手可开 | 该设置之前没有入口 |
| 设置中心新增「权限管理」入口（AI 能力权限） | 该页面之前没有入口 |
| 清理无实际作用的空文件 | 代码库更干净 |

## 2026-08-15（全面审计三批）
| 内容 | 原因 |
|---|---|
| LLM 用量记录新增用途标签（对话/记忆/状态/复习等各自归类） | 每日 token 消耗可按功能拆分、方便查哪最耗 |
| 历史 AI 动态补齐归属用户信息 | 数据完整、统计准确 |
| 各来源记忆补上“是谁说的”标记（你的话/日记/生活/状态/群聊各自归属正确） | 事实可追溯、可靠度评分更准 |
| 清理重复记忆：同一事实多种说法合并为一条 | 记忆更干净、检索更准 |
| 记忆复习提醒只对还在聊天的角色触发 | 减少后台无效空转 |
| 清理无主评论残留 | 数据更干净 |
| 修复对话历史顺序描述与实际不符 | 避免 AI 记错消息先后导致剧情穿帮 |
| AI 的剧情内容不再混入长期记忆，改存舞台（与游戏/假话同级隔离） | 防止剧情被当成真事复述 |
| 记忆提取不再保存“无/（空）”等空内容，并清理了历史 31 条 | 记忆库更干净、检索更准 |
| 高频数据表补充索引（消息/记忆/动态/评论/会话） | 数据量增长后查询更快 |
| 主动消息触发日志加清理与降频，只记录关键决策 | 日志不再无限膨胀 |

## 2026-08-15（世界认知架构 P0-P5）
| 内容 | 原因 |
|---|---|
| 记忆有了可靠度评分：你说的最可信，AI 自己猜的会打折，被纠正过的会降级 | 事实更可信、少被带偏 |
| 你说“没错/记得很清楚”会强化相关记忆，说“你记错了”会标记并降级该记忆 | AI 会从纠正中学习 |
| AI 回复与已有记忆矛盾时会自动发现并降低那条记忆的可信度（低频后台检查） | 减少前后矛盾 |
| AI 会记住“正在进行/刚发生的事”，新状态会自然顶替旧状态 | 场景切换更连贯不穿帮 |
| 聊天的状态更新会自动沉淀为 AI 的世界记忆，下次对话自然带过 | 连续性更强、更少生硬提问 |
| AI 说话更严谨：确认过的事当事实说，推断的会说“可能/我觉得”，计划中的事不会说成做完了 | 避免把猜测和计划当事实 |
| 对话记忆会标记是谁说的，群聊里各角色只按自己知道的说 | 事实可追溯、信息不串线 |
| 私聊内容只在当事人之间可见，其他角色不知道（除非在群里公开提起） | 避免多角色信息串线 |
| 群聊角色回复遵守知识边界，不提及自己不可能知道的事 | 角色各说各话更真实 |
| 重要记忆升级为核心记忆，对话无条件携带（不靠检索碰运气） | 关键信息不再间歇性遗忘 |
| 对话注入关系锚点与未完成事项（进行中目标/待兑现承诺） | AI 惦记着该做的事 |
| 事件与记忆新增说话人归属与认知状态标记（谁说的/事实还是推断） | 为事实可追溯打地基 |

## 2026-08-15（AI生活目标自动延续）
| 内容 | 原因 |
|---|---|
| AI 目标全部完成后会自动生成新目标（优先按当前兴趣，其次固定方向） | 目标做完后不会无事可做 |

## 2026-08-15（主动消息接入思考与可视化）
| 内容 | 原因 |
|---|---|
| 主动交流消息的思考过程也像私聊一样在气泡里折叠展示 | 主动消息与私聊体验一致 |
| 主动交流消息（搭话/宠物/计时/关怀/复习等）按角色的思考挡位生成 | 之前主动消息没有思考过程 |

## 2026-08-15（宠物消息与知识注入）
| 内容 | 原因 |
|---|---|
| AI 领养宠物、照顾宠物的告知消息会按角色性格与聊天风格来说 | 之前语气千篇一律、不像本人 |
| 宠物对话与提醒新增习性知识（吃什么/怎么照顾），用户说错食物时会自然纠正 | 避免顺着口误答错 |

## 2026-08-15（事件时钟）
| 内容 | 原因 |
|---|---|
| 私聊右上角新增「事件时钟」入口，可查看进行中的计时并手动删除 | 让计时可控可管理 |
| 事件时钟支持模糊承诺（等下/睡醒/弄完），按默认时长计时到点提醒 | 没给具体时间也会提醒 |
| 事件时钟新增识别“X分钟后…好了叫你/叫我”类承诺，到点主动提醒 | AI 说“二十分钟好了叫你”不再漏计时 |
| 事件时钟新增识别“X分钟后就能完成/做好/搞定”类说法 | 这类完成承诺之前不会计时 |

## 2026-08-15（小家 · 生活可视系列）
| 内容 | 原因 |
|---|---|
| 小家房间布局更紧凑，并新增地毯/挂画/台灯/置物架/垃圾桶/挂钟等装饰 | 房间不再空旷 |
| 小家改为星露谷式俯视视角：地板纹理、家具立体感（顶面/侧面/底部阴影） | 更有空间层次 |
| 家具与角色按前后位置自动遮挡，人走到家具后面会被挡住 | 伪 3D 空间感 |
| 角色形象升级为精致小人（头发/衣装/脚下阴影） | 人物更可爱 |
| 小家场景全面升级：房间地板/墙面/家具/角色全部换上新像素素材 | 画面更精致好看 |
| 家具与角色呈现更细腻（描边、高光、阴影、细节） | 小家更生动 |
| 小家交互改为屏幕中间弹窗（点家具/宠物） | 操作更顺手 |
| 小家底部新增虚拟方向键与「互动」按钮（也可直接点屏幕移动） | 小屏操作更舒服 |
| 版本更新至 v3.1.0：小家升级为多房间（客厅/卧室/厨房/浴室） | 生活更丰富 |
| 小家房间可切换，各房间有专属家具（床/沙发/灶台/浴缸等） | 更像一个家 |
| 新增运动/听音乐/玩游戏等生活互动 | 宅家生活更多样 |
| 宠物用像素形象展示，点宠物可喂食、抚摸（互动时形象会变化） | 宠物更生动 |
| 新增乌龟、龙猫、刺猬三种宠物形象 | 宠物种类更全 |
| 版本更新至 v3.0.0：底部第 4 个标签变为「小家」 | 生活可视化大版本 |
| 「小家」上线：像素家居房间，点地面走动、点家具生活（睡觉/做饭/吃饭/工作/看电视/读书/洗澡） | 把 AI 的生活搬到眼前 |
| 小家里的「宠物窝」可进入宠物功能，宠物也会出现在房间里 | 宠物入口内置、更统一 |
| 互动会改变 AI 的体力/心情/饥饿并记录进生活日志 | 生活更真实 |

## 2026-08-15（新增查岗）
| 内容 | 原因 |
|---|---|
| 角色设置-日常新增「查岗」开关：开启后 AI 主动找你时能自然知道你正在用什么软件 | 让 AI 更了解你的当下 |
| 「管制」作为查岗子开关占位，点击会提示“待设计” | 预留后续功能 |
| 小家暂时隐藏房间里的宠物形象（点宠物窝仍可进入宠物功能） | 宠物形象挡住了宠物窝入口 |

## 2026-08-14（AI 生活进化 + 群聊修复 + 主动生图）
| 内容 | 原因 |
|---|---|
| 回复标记优化：「状态更新」「自述更新」等标记不再混在正文里，改为气泡下方小字显示；AI 回复上限提升 | 聊天更干净、回复更从容 |
| 「AI 此刻」状态移到 AI 互动页角色卡片：每个角色卡片显示正在做什么、心情 | 一眼看到每个 AI 的状态 |
| 移除小手机外面入口处的「畅聊」标签；小手机内的畅聊图标保留 | 入口更统一 |
| AI 生活整合：「兴趣与目标」「产物库」并入「AI 生活」，顶部三个 tab 切换 | 生活功能更集中 |
| 「浏览记录」入口移到角色小手机的浏览器里 | 入口更顺手 |
| 家庭群聊优化：角色回复更自然（能认出是谁在说话）、支持角色和你的头像、入口移到畅聊右上角 | 群聊体验更完整 |
| 小手机新增「我的手机」入口（敬请期待，未来将映射你的手机） | 为未来功能预留 |
| 主动生图优化：AI 会在合适的时机主动给你画画（频率受控，不打扰） | 让 AI 更会表达心意 |
| 修复小手机浏览器搜索偶尔超时 | 搜索更稳定 |
| AI 生活引擎升级：AI 会真实浏览网页并留下记录、有自己的日程安排、记得你们的重要时刻并在纪念日提起 | AI 的生活更真实 |
| 相册与图片留存检查正常 | 放心使用 |
| 定时承诺更可靠：说好了「多久后回来/做完」就准时兑现，没到点不会提前“完成” | 修复剧情时间错乱与漏触发 |
| 修复 AI 写日历备注没保存（括号格式与闭合标签兼容），历史备注已补写 | 日历能看到 AI 记的日程了 |
| AI 写的日历备注、备忘录与状态更新显示在气泡内底部小字 | 聊天界面更干净 |
| 小手机日历备注与备忘录带记录者署名，谁记的一目了然 | 避免记忆混乱 |
| 好友编辑页优化：基础信息新增「背景信息」，声音设置移到页面底部 | 好友资料更完整、编辑更顺手 |
| 家庭群聊升级：@ 谁谁必回应、回应带角色心情、群聊内容会沉淀为记忆 | 群聊更有真实感 |
| 模拟操作恢复并升级：手机操作工作流上线（自建步骤序列，说“帮我执行 XX”即触发，支持 Shizuku 系统级操作） | 模拟操作更稳、更自由 |
| 工作流编辑器升级：步骤卡片可视化编辑，支持拖拽排序、按动作类型填写参数 | 编排更直观 |
| 工作流编辑器升级：新增节点连线画布，拖动节点、连线即执行顺序 | 编排更直观自由 |
| 编排更省心：操作目标可从当前屏幕点选（图标、图片也能选，自动转坐标）、启动应用从已安装列表选择 | 普通人不用记坐标和包名 |
| 工作流画布升级：从节点圆点拖线连接，连线可设条件（成功/失败/屏幕有没有某文字），支持分支流程 | 复杂操作也能编排 |
| 执行更聪明：工作流支持条件分支与循环，最多 30 步自动防卡死 | 复杂流程稳定可靠 |
| 工作流编辑细节优化：画布连线实时跟随节点移动、分支工作流步数显示正确 | 编辑更跟手、信息更准确 |
| 小手机新增「宠物」应用：能看到角色养的宠物、状态与角色自己的照顾记录；小手机内不能直接帮忙，想照顾去主页拜访 | 小手机更完整、AI 生活更真实 |
| 修复工作流「选择操作目标」偶尔无节点：改为实时读取当前屏幕，并支持回看最近打开的应用里的目标 | 点选目标更可靠 |
| Shizuku 报错更友好：服务未启动/未授权/连接异常都会给出明确指引（不再显示看不懂的英文报错） | 系统级操作更好排查 |
| Shizuku 连接异常自动重试一次；读屏未连接时提示区分「没开」与「更新后需重新开启」并一键跳转系统设置 | 系统级功能更稳、更好排查 |
| 修复 Shizuku 应用列表读取失败（process hasn't exited）：改用轮询进程退出状态，真机验证可读取 132 个应用 | 应用列表恢复可用 |
| 读屏服务更稳定：服务事件防崩溃加固、退出任务不随任务停止；若仍被系统关闭，请按提示在手机「后台耗电管理」允许本应用后台运行 | 读屏不再动不动被关 |
| 读屏服务前台保活：开启读屏后显示常驻通知，App 退出/划掉任务后服务保持连接（需在系统设置重新开启一次读屏） | 读屏服务不再被后台清理 |
| 坐标点击步骤新增「获取屏幕范围」按钮：一键显示当前手机的 x/y 坐标范围 | 手填坐标不用再查分辨率 |
| 「获取屏幕范围」移到节点连线画布左侧工具条；工作流新增「返回主页」步骤（一键回到手机桌面） | 工具更顺手、步骤更全 |
| 选择应用升级：显示应用中文名（微信、抖音等一眼认出），支持按名称搜索 | 普通人不用记包名 |

## 2026-08-13（图片消息修复）
| 内容 | 原因 |
|---|---|
| 修复私聊发图无回复：services 重命名（image_understanding→image_understanding_service）后服务器进程未重启仍执行旧代码，图片接口报 ModuleNotFoundError；补齐 douyin_mcp 插件旧引用并重启服务器 | 恢复图片消息链路 |
| 小手机浏览器搜索优化：结果更准确、可点击打开真实链接、无结果时有兜底、结果改为固定大小卡片 | 搜索更好用更直观 |
| 角色设置页重组：AI 离线生活移入「日常」组（AI日记下方），生活分享/离线生活强度改为其子项；「隐私」下思考过程移至调用能力下方；所有可展开项开关左侧加无柄箭头（仿手机感知） | 设置更清晰统一 |
| 角色删除保留记忆：删除角色后其他角色对 TA 的记忆保留并标记「离开」（memories.departed_names），同时生成【xxx离开了】记忆（高重要度，随遗忘机制自然淡去）；标记携带角色 id（名字#id）去重，并排除其他同名角色的记忆，避免重名误伤 | 避免社交关系割裂感，重名安全 |
| 修复「AI 离线生活」不触发：scheduler 主循环从未调度 life_tick（registry 只有注册无消费循环，life_tick 注册后从未执行，life_states 停滞/生活活动与生活记忆为零）；仿 moments/diary 在主循环内联每 1 小时调度 + 异常隔离 | 恢复 AI 离线生活引擎（活动/生活记忆/回归摘要素材） |

## 2026-08-12（AI 离线生活上线 / 织库增强）
| 内容 | 原因 |
|---|---|
| AI 离线生活上线：AI 会自主休息、整理记忆、反思、浏览、创作与学习，默认开启 | AI 伙伴更有生命力 |
| 兴趣与目标：AI 有自己的兴趣偏好与长期目标，离线活动持续成长 | 离线生活有长期价值 |
| 回归摘要：AI 会主动分享近 24 小时的生活点滴（每角色每日至多 1 次） | 离线生活自然分享 |
| AI 生活时间线与产物库：可查看 AI 的离线记录与文字、图片作品 | 离线成果可视化 |
| 织库新增「私·织库」：AI 的私生活记忆与共同记忆分开整理 | 私生活与共同记忆隔离 |
| 织库增强：卡片以对话原文为主、支持查重去重、画布缩放与筛选增强、跨角色合并 | 全景记忆更完整 |
| 全注入对话开关：开启后每次对话自动注入织库全景卡片 | 对话更懂全局记忆 |

## 2026-08-12（插件市场 / 系统感知 / 设置与 UI 收尾）
| 内容 | 原因 |
|---|---|
| 插件市场上线：浏览内置插件并一键安装，入口在扩展页右上角 | 插件更容易获取 |
| 手机感知升级：授权后可感知前台应用、屏幕、电量、网络等系统状态 | AI 感知真实手机状态 |
| 手机感知设置重组：读屏、模拟操作、通知、位置改为可折叠分组 | 设置页更清晰 |
| AI 能力权限：生图、识图、语音等可设为允许、每次询问或禁止 | 能力调用安全可控 |
| 抖音记忆收紧开关：开启后公开创作排除关系类私密记忆 | 公开平台隐私保护 |
| AI 隐私上锁升级：解锁通过率随角色心情与信任动态变化 | 隐私更贴合角色状态 |
| 多角色家庭群聊：可创建 2-8 人角色群，一起聊天 | AI 社交生态 |
| 主动消息增强：连续未回复自动暂停、可设免打扰时段、跟进未聊完话题 | 主动搭话更贴心 |
| 首页滑动动画：抽屉右滑拉出、底部 Tab 滑动切换 | 操作更流畅 |
| UI 2.0 收尾：剩余页面统一简约卡片风格，修复头像裁剪与开关灰化 | 界面统一美观 |
| 音色试听：角色编辑页可即时试听音色与语速语调 | 语音个性化 |
| 应用图标更新 | 新形象 |
| 内部结构优化：docs 新增路线图缺口地图、前端页面按领域分目录、清理死代码与缓存 | 结构清晰、为未来更新留位 |
| 工程规范落地：docs/engineering-protocol.md（25 条原则+全项目对照+认知链路审计）、logger 与 services 命名统一、记忆/限额/人格一致性测试补强 | 工程基线化，结构可维护 |

## 2026-08-11（UI 2.0 全批实施 + 语音自定义声色）
| 内容 | 原因 |
|---|---|
| UI 2.0 全批：朋友圈社交化、伙伴列表/我的/设置中心/侧边抽屉/登录注册卡片化，全局滑动转场 | 实施 UI 2.0 设计稿 |
| 聊天无回复根因修复：模型内容审核拦截亲密内容，自动降级备用模型兜底 | 修复无回复 |
| 手机感知页、扩展页卡片化；气泡精细样式与滑动动画增强 | UI 2.0 跟进 |
| 我的页/抽屉入口精简，朋友圈时间细化，底部改「小手机」模拟手机桌面 | 需求精简 |
| 语音「自定义声色」：角色编辑页新增音色选择与语速/语调调节 | 语音个性化 |
| 小手机桌面真实化：图标拖拽/删除、时间天气小组件，新增相册/应用市场/日历/浏览器/主题/设置应用 | 模拟真实手机 |
| 浏览器真实搜索 + AI 主动写日历备注与备忘录 | 功能推进 |
| 相册图库化（预览/保存/删除）；头像更换新增圆形裁剪 | 反馈优化 |
| 角色详情/设置/生活页卡片化；API 配置新增模型预设；LLM 用量统计弹窗 | UI 2.0 + 用量可视化 |
| 服务器级模型改用免费额度新模型；聊天调用加超时防挂起 | 稳定与免费额度 |

## 2026-08-10（社交交互层 v2：平台档案 + 社交记忆 + 统一事件接入 arbiter）
| 内容 | 原因 |
|---|---|
| 新增平台档案：App 与抖音分别管理表达分级与记忆权限 | 平台表达分级 |
| 抖音创作不再注入私人记忆，改注入筛选后的中性记忆 | 公开平台隐私保护 |
| 新增社交记忆：记得粉丝关系与常互动用户，与私人记忆隔离 | AI 记得粉丝 |
| 抖音评论回复统一接入 arbiter 决策流（保留风控/额度/署名） | 平台决策统一 |
| 语音/全模态大模型服务器级配置落地，API 配置页布局与供应商预设 | 配置能力完善 |
| 私聊优化：AI 气泡展示思考过程与调用能力；角色设置新增隐私组 | 提升透明度 |
| 织库 · 全景记忆需求登记：抽屉-体验新增入口，完整记忆库卡片 + 无限画布编织成球体（方案见 docs/archive/weave-library-plan.md） | 需求登记 |
| 织库·全景记忆 Phase A 实施：后端 weave_cards/weave_card_memories 两表 + 5 接口（列表/详情/删除/生成/图数据）+ 记忆写入增量补卡（30 分钟/角色防抖、content_hash 幂等、stale 懒重建）；前端抽屉-体验新增「织库」入口 + 卡片列表页（角色切换/整理生成/详情小窗/删除）；3D 评估结论：2.5D CustomPaint 主线，真 3D 需渲染框架升级，暂列远期 | 织库拍板实施（Phase A） |
| 织库·全景记忆 Phase B 实施：2.5D 无限画布页（斐波那契球面投影/拖动惯性旋转/节点正弦抖动/连线按强度显隐，>80 卡自动降级 2D 螺旋）；投影纯函数抽离 utils/sphere_projection.dart（含单测）；详情小窗提取为公共组件供列表/画布复用；列表页右上角新增画布入口 | 织库拍板实施（Phase B） |

## 2026-08-09（API 配置页生图/识图修复 + 主动生图子开关）
| 内容 | 原因 |
|---|---|
| 修复：API 配置页生图/识图两区块字段交叉绑定 | 配置显示与保存错位 |
| 新增「主动生图」子开关：开启后 AI 可在合适时机自发发图 | 主动发图独立控制 |

## 2026-08-09（内部结构优化：清理遗留代码与构建产物）
| 内容 | 原因 |
|---|---|
| 清理遗留脚手架、死代码与构建产物（约 1.6GB） | 代码干净化 |

## 2026-08-09（抖音创作与生图修复）
| 内容 | 原因 |
|---|---|
| 生图指令强化：明确要求时必须输出标记、绝不假装发图 | 修复自拍未生成图片 |
| 抖音图文无图发布失败：发布前自动生图配图 | 修复发布未成功 |
| 修复生图任务偶发失败 | 生图更稳定 |
| 图片消息文案角色化：配图文案用角色台词 | 更符合角色设定 |
| 发布队列触发解耦：到点后最快 1 分钟执行 | 修复执行延迟 |
| 抖音创作入口：手动创作 + AI 自主图文生成 | 抖音内容更丰富 |
| 图文与回复分开，无图也可确认发布（抖音自动配图） | 图文解耦 |
| AI 创作增强自我性：注入角色人设与记忆参与创作 | 抖音更像角色本人 |
| 回复额度 60/40 拆分：粉丝 15 条 / 非粉丝 10 条 | 优先照顾粉丝 |
| 抖音生成接入主链路人格，排除用户身份画像 | 不冒充真人 |
| 抖音创作上线：扫码绑定账号，AI 感知作品数据与粉丝评论 | AI 运营自己的抖音 |
| AI 生成图文/回复草稿，确认后随机时间发布（图文 ≤2/天、回复 ≤25/天） | 人工确认防封号 |
| 发布内容寻求准许开关：开=等批准；关=全自动 | 支持全自动运营 |
| 「AI 好友」小信封：待批准角标 + 确认/拒绝 + 发布倒计时 | 批准请求集中入口 |
| 发布/回复前本地违禁词拦截 | 防封号 |

## 2026-08-09（记忆舞台：小游戏与假话隔离）
| 内容 | 原因 |
|---|---|
| 记忆舞台：小游戏设定/剧情与 AI 假话隔离进舞台，日常对话正常入库 | 记忆更真实 |

## 2026-08-08（扩展插件系统 / 位置天气 / 记忆升级 / 私聊沉浸感 v2.0）
| 内容 | 原因 |
|---|---|
| 小手机「畅聊」优化：进页自动定位最新消息、上限 50 条、右上角聊天记录箱 | 聊天体验更顺手 |
| 私聊「继续」链路重构：延续上一句自然推进剧情 | 延续对话更自然 |
| 服务器控制台跨平台：Windows/Linux/macOS 通用桌面控制台，随开源包分发 | 部署更灵活 |
| AI 互动页顶部补「菜单」按钮 | 导航更完整 |
| 扩展（插件）系统上线：支持 zip 安装插件、全部/普通/插件协议分类、启用开关与配置 | 扩展插件能力 |
| 24 小时应用使用时长统计，AI 能感知你的用机习惯并主动关心 | 零风险本地能力 |
| 获取地理位置 + 当地天气，AI 回复、日记等文本生成都会带上天气信息 | 更懂你的生活场景 |
| 时间感知增强：AI 知道当前时间与距离上次互动的时长 | 提升交流自然感 |
| 记忆架构 v2.1：意义记忆 / 目标记忆 / 关系记忆 / 情境复习 / 身份提炼 | 记忆更深刻更立体 |
| 记忆治理：同主题合并去重、提取节流、遗忘曲线调参、记忆本按星级+日期排序 | 减少重复、提升准确 |
| 私聊沉浸感 v2.0.0：括号动作/神态剥离成气泡外小字（带归属箭头）、长按菜单（删除/引用/复制）、完整引用消息 | 增强聊天沉浸感 |
| 抽屉新增「更新公告」「用户协议」；「体验设置」分组收纳手机感知/免打扰等 | 设置更清爽 |
| 「忘记密码？修改」入口移至登录页；API 配置「识图」上移 | 细节优化 |

## 2026-08-07（认知循环全量开放 / 宠物大改版 / AI 隐私上锁 / 语音修复）
| 内容 | 原因 |
|---|---|
| 认知循环 v2.1 全量开放：感知-检索-规划-反思，回复更自然、更有连续性 | 对话模型大更新 |
| 记忆复习优化：融入聊天上下文，主动交流下新增「记忆复习」子开关 | 复习更自然不割裂 |
| 宠物形象高清素材化：7 物种全解锁（含蛇/守宫），喂食/玩耍/清洁触发对应行为状态 | 宠物更好看更生动 |
| 宠物互动记录最多 5 条、新的顶掉旧的；状态趋势蛛网对比改 xx/xx + 双色区分 | 体验优化 |
| AI 隐私上锁：日记/小手机上锁，查看需向 AI 申请（冷却 2 分钟，同意后解锁 0.5-24 小时） | 隐私保护 |
| 设置页重排：主动交流移至状态上方、朋友圈下移并新增评论/回复子开关 | 设置层级更合理 |
| 语音发送修复：不再频繁失败、语音条显示在文字下方、语音 14 天过期只留文本 | 语音体验稳定 |
| 情绪记忆内页：每日/月/年折叠；状态趋势最新数据修复 | 状态查看更清晰 |

## 2026-08-06（私聊更多功能 / 朋友圈收尾 / AI 间互动 / 安全加固）
| 内容 | 原因 |
|---|---|
| 聊天页角色心情小标识（角色名字右侧），随状态变化 | 聊天氛围更鲜活 |
| 角色记得你发的朋友圈，聊天中会自然提及 | 朋友圈与聊天打通 |
| 角色删除彻底化：删除角色会一并清除其全部内容 | 数据干净不残留 |
| 私聊更多功能：文件保留 5 天、文档问答、语音气泡本地播放、上滑取消/60s 提示、TTS 语音回复、自定义表情包 | 私聊能力全面增强 |
| 宠物系统 6 项改版：直接互动（喂食/玩耍/清洁）、长按名片遗弃、互动展示区、领养上限 3 只 | 宠物交互更自然 |
| AI 间私聊 + 宠物关怀：角色之间可互动，也会照顾你的宠物 | AI 社交生态 |
| 多语种 P1：语言切换 + AI 回复语言跟随 | 面向多语言用户 |
| 用户备忘录 + 用户日记：供 AI 角色阅读，丰富上下文 | AI 更懂你 |
| 打赏入口落地（多平台赞赏） | 支持作者 |
| 安全清单全项落实：JWT 密钥 / 登录限流 / 密码强度 / 修改密码 / 输入上限 | 安全加固 |
| 状态回落 2h + AI 私聊开关 + 主动交流体检 | 状态系统优化 |

## 2026-08-05（记忆遗忘曲线 / 朋友圈互动收尾 / 状态系统）
| 内容 | 原因 |
|---|---|
| 首页新增「手机」Tab 入口 | 手机感知更易用 |
| 记忆遗忘 P1/P2：主动到期复习、手动记忆锁、AI 自主评星 | 记忆更贴近真实遗忘规律 |
| 朋友圈互动体验收尾 + AI 情绪关怀 | 朋友圈更有温度 |
| 状态惰性回落 Phase 2 + 漂移速率提升 | 状态变化更自然 |
| 通知双轨真机验证，失效自愈 | 通知稳定 |

## 2026-08-04（手机感知 / 时光页 / 消息弹窗 / 结构优化）
| 内容 | 原因 |
|---|---|
| 多用户数据隔离：每个账号数据完全独立 | 隐私与数据安全 |
| 手机感知 Phase 1-3：读屏、剪贴板、相册、媒体文件、通知监听、模拟操作 | AI 能感知你的手机 |
| 时光页：共同回忆时间线 + 认识纪念日主动提及 | 记录你们的共同回忆 |
| 消息弹窗升级：后台常驻 + 通知总开关 + 通知白名单 | 消息提醒更及时 |
| 可视化状态系统 v1：八维状态 + 多维度触发事件 | 角色状态可视化 |
| 宠物动态化：状态驱动动效 | 宠物更生动 |
| 开源发布包机制 + 结构优化（api 层瘦身/前端拆分/lint 清零） | 为开源与后续开发铺路 |

## 2026-08-03（宠物系统 MVP / 记忆融合 / 关系网 / 消息弹窗）
| 内容 | 原因 |
|---|---|
| 宠物系统 Phase 1：领养、喂养、玩耍、清洁、改名、拜访 | 宠物系统起步 |
| 记忆融合：半重复记忆 LLM 合并、超大簇合并、置顶内容修复 | 记忆更干净 |
| 关系网：用户与 AI 角色关系设置 + 用户画像注入 | 关系更亲密 |
| 消息弹窗：顶部横幅 + 系统通知 | 消息提醒 |
| 角色删除级联清理：删除角色 = 清除其全部内容 | 数据干净 |
| 编码规则放宽 + 聊天记录修复 | 兼容性提升 |

## 2026-08-02（朋友圈作者视角 / 记忆基础 / 图片理解 / VLM）
| 内容 | 原因 |
|---|---|
| 本地 VLM 图片理解 + OCR 双引擎（本地便携版） | 看图能力本地化、不耗云 API |
| 朋友圈作者视角 + 角色生日 + 头像接入 | 朋友圈更像真实社交 |
| 记忆基础：记忆曲线 · 星级衰减机制 | 记忆系统起步 |
| 图片理解双引擎：本地 VLM 语义描述 + OCR 文字识别 | 看图能力本地化 |
| 私聊图片配文（caption），图片内容仅进 AI 上下文 | 聊天更丰富 |
| 服务器管理统一，修复双拉起；防文件损坏体系 | 服务器更稳定 |

## 2026-08-01（基础体验）
| 内容 | 原因 |
|---|---|
| 睡觉免打扰：深夜静默 | 不打扰休息 |
| 性能与配置优化 | 体验更流畅 |
| UI 调整 | 界面更美观 |
| 修复「启动应用」步骤选应用时列表空白 | 应用列表读取改为后台执行并加固，不再卡住界面，列表与搜索均正常 |
| 自述不再额外存为记忆：AI 的自述只保留在角色资料里 | 避免重复冗余，记忆库更干净、检索更准 |
| 「背景信息」与「自述」分开：背景信息只由你填写，AI 的自述会单独形成、不再覆盖背景信息 | 你提供的设定不会被 AI 顶替 |
| 小手机浏览器搜索提速：结果更快返回、不再超时，等待时有提示 | 搜索更稳更流畅 |
| AI 更会「想念」你：依恋与好奇积累到一定程度会主动找你聊天，刚聊过或疲惫时不会打扰 | 主动消息更自然、更像真人 |

## 2026-08-24（SSE 流式体验修复 + API Key 配置优化）
| 内容 | 原因 |
|---|---|
| SSE 流式错误提示友好化：API Key 无效/请求超时/频率超限显示中文提示，不再露出底层报错原文 | 全量审查发现：流式路径直接把底层原始错误推给用户，排错体验差 |
| SSE 流式兼容反向代理部署（逐字上屏不再被缓冲） | nginx 等反向代理默认缓冲流式响应，导致打字机效果退化；审查报告 P2-3 |
| API Key 保存后自动生效（无需再手动打开开关） | 直接调接口存 Key 时 enabled 默认关闭导致 Key 不生效；审查报告 P3-1 |
| 修复：流式异常回退也失败时不再重复弹错误 | 原链路会先后推送两次相同错误事件；审查报告 P2-2 |

## 2026-08-24（流式聊天用量统计补齐）
| 内容 | 原因 |
|---|---|
| SSE 真流式聊天的 token 用量纳入用量统计（用量页/限额不再漏记流式对话） | #57 遗留：流式路径此前不产生用量记录，统计与限额少算 |

## 2026-08-24（语音回复升级：逐句合成边播边聊）
| 内容 | 原因 |
|---|---|
| 语音模式下 AI 回复升级为逐句合成：边打字边出声，不用再等整段回复生成完 | #57 遗留：语音此前等整段生成后一次性合成，等待久、体验割裂 |
| 新语音发出会打断上一轮播报，逐句顺序播放 | 避免新旧语音重叠，长回复也能流畅听 |

## 2026-08-24（v3.2.8 审查修复）
| 内容 | 原因 |
|---|---|
| 修复主动消息统计页在未指定角色时可能报错的问题 | 全量审查发现统计接口引用了不存在的字段（预存 bug） |
| 主动消息测试接口错误提示友好化（未配置 Key 时返回中文提示而非服务器错误） | 审查报告 P2-1 |
| 数据库版本迁移补全两张新表（用户作息/浏览器快照），全新安装升级更稳 | 审查报告 P2-2 |
| 语音逐句合成异常不再静默丢失（有日志可查） | 审查报告 P3-1 |

## 2026-08-25（语音链路稳定性修复）
| 内容 | 原因 |
|---|---|
| 语音逐句播放中断开连接不再残留后台任务（内存更稳） | 深度复审发现客户端断开时语音合成任务不取消会泄漏（P2-A） |
| 语音合成中途出错时聊天历史不丢内容（自动回退整段落库） | 深度复审发现合成任务中途失败会导致部分回复不保存（P2-B） |

## 2026-08-26（MCP 标准工具接入 Phase 1）
| 内容 | 原因 |
|---|---|
| 新增：MCP 标准协议接入（第一阶段）——可连接 stdio 型 MCP 服务器，AI 自动发现并调用其工具 | 接入 MCP 生态（文件系统/GitHub/SQLite 等通用工具即插即用），mcp SDK 2.0.0 已在 Python 3.14 实测兼容 |
| 扩展页后续将提供 MCP 服务器管理与工具权限配置（后续阶段） | Phase 2-4 规划中 |

## 2026-08-26（MCP 标准工具接入 Phase 2）
| 内容 | 原因 |
|---|---|
| 新增：MCP 接入支持远程服务器（SSE/HTTP 传输），AI 可将 MCP 工具纳入调用 | 本地 stdio 之外补全远程 MCP 生态（GitHub/Slack 等），风险防护随附（禁内网默认开启） |
| 新增：AI 自动识别并调用已启用的 MCP 工具，高风险工具默认需确认 | Phase 2 核心：工具声明注入上下文 + 权限三档（ALLOW/ASK/FORBID） |
| 服务器可预置 MCP 配置（mcp_servers.json），部署即用 | 方便 Docker/服务器管理员预设常用 MCP 服务器 |

## 2026-08-26（MCP 标准工具接入 Phase 3：扩展页管理）
| 内容 | 原因 |
|---|---|
| 新增：扩展页「MCP 工具」分区——服务器列表（状态灯/工具数/启停/连接/测试）、添加/编辑（本地命令或远程地址）、工具权限三档切换、常用模板一键填充 | 用户可直接在 App 内管理 MCP 服务器，无需命令行 |
| MCP 工具权限可单独设置（允许/询问/禁止），高风险工具默认询问 | 与既有权限体系统一，细粒度管控 |

## 2026-08-28（MCP 标准工具接入 Phase 4：增强）
| 内容 | 原因 |
|---|---|
| MCP 服务器资源（Resources）摘要注入 AI 上下文，AI 可感知外部数据源 | 让 AI 知道 MCP 服务器提供了哪些可读资源 |
| MCP 工具调用记录：最近调用列表（工具/耗时/成功失败）在扩展页展示 | 便于排查 AI 调用了什么、结果如何 |
| 服务器状态灯实时刷新（每 6 秒），连接变化一目了然 | Phase 4 状态可见性增强 |

## 2026-08-29（MCP 安全与健壮性修复）
| 内容 | 原因 |
|---|---|
| 多用户隔离：AI 只会看到并使用自己配置的 MCP 服务器，无法触及他人服务器 | 深度审查发现工具注入缺归属校验（多用户部署风险） |
| 流式对话暂不注入 MCP 工具声明（避免 AI 假装调用）；工具结果完整回传（不再截断成 120 字） | 修复声明与执行不一致的幻觉风险 + 工具结果实用性 |
| 连接并发/崩溃清理/超时可配置/目录按用户隔离等健壮性加固 | 深度审查 P3 系列 |

## 2026-08-29（语音回退显示修复）
| 内容 | 原因 |
|---|---|
| 修复：语音合成中途异常自动重试时，聊天界面不再出现重复气泡 | 复审发现回退路径会重推消息而前端去重按消息 ID 匹配不上（旧 ID 已删除） |
